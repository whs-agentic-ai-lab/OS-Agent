# 0829 OS 툴 현황

## 1. 전체 현황

| 상태 | Tool | Action |
|---|---:|---:|
| MD 카탈로그 | 129 | 383 |
| `ToolDefinition` 코드 작성 | 129 | 383 |
| handler/verifier/resetter 등록 | 129 | 383 |
| 현재 Agent `implemented_actions` 노출 | 5 | 20 |
| Linux Host/Container 실실행 검증 | 미완료 | 미완료 |
| EC2 인증 | 미확인 | 등록 근거 없음 |

코드는 383개 Action까지 작성됐지만 실제 Agent가 구현 완료로 노출하는 것은 20개다.

`ToolDefinition`이 존재하는 것과 실제 환경 인증이 완료된 것은 구분해야 한다. 실제 Linux Host/Container 실행, 독립 Verifier 재조회, rollback 복구 검증을 통과하지 않은 Action은 코드 존재만으로 `implemented_actions`에 추가하지 않는다.

## 2. Family별 Definition

| Family | Tool | Action |
|---|---:|---:|
| Identity·Capability | 7 | 23 |
| File·FD | 13 | 49 |
| Exec·Privilege | 10 | 21 |
| Mount·Filesystem | 8 | 26 |
| Process·IPC | 14 | 51 |
| Namespace·Kernel | 16 | 54 |
| systemd·Privilege | 9 | 26 |
| Docker·containerd·OCI | 16 | 47 |
| Persistence | 28 | 66 |
| Audit·Evidence | 8 | 20 |
| 합계 | **129** | **383** |

## 3. 현재 실제 Agent 노출 Tool

현재 `implemented_actions`에 등록된 범위는 5개 Tool, 20개 Action이다.

```text
privilege.identity_probe
  setuid
  seteuid
  setfsuid
  setgid
  setegid
  setfsgid
  setgroups

privilege.no_new_privs_probe
  enable

file.content
  read
  write
  append
  truncate

sudo.run
  list
  run_probe

process.procfs
  read_environ
  read_cmdline
  read_maps
  list_fd
  read_root
  read_cwd
```

## 4. 작성된 전체 Tool 목록

### 4.1 Identity·Capability — 7개 Tool / 23개 Action

```text
privilege.identity_probe
privilege.capability_probe
privilege.securebits_probe
privilege.no_new_privs_probe
keyring.manage
session.manage
umask.set
```

### 4.2 File·FD — 13개 Tool / 49개 Action

```text
file.open
file.create
file.content
file.remove
file.move_link
file.metadata
file.acl
file.xattr
file.inode_flags
file.lock_lease
file.open_by_handle
fd.operate
fd.transfer
```

### 4.3 Exec·Privilege — 10개 Tool / 21개 Action

```text
exec.run
exec.with_environment
exec.privilege_transition
filecap.manage
sudo.run
polkit.invoke
dbus.call
supervisor.request
toolchain.build
chroot.run
```

### 4.4 Mount·Filesystem — 8개 Tool / 26개 Action

```text
mount.manage
mount.bind
mount.tmpfs
mount.idmap
mount.overlay
filesystem.policy_probe
filesystem.resource_pressure
volume.local_manage
```

### 4.5 Process·IPC — 14개 Tool / 51개 Action

```text
process.spawn
process.signal
process.ptrace
process.memory
process.procfs
process.security_state
process.pidfd
process.schedule
memory.lock
unix_socket.manage
unix_socket.fd_transfer
ipc.sysv
ipc.posix
process.accounting
```

### 4.6 Namespace·Kernel — 16개 Tool / 54개 Action

```text
namespace.manage
namespace.handle
seccomp.install
seccomp.notification
landlock.restrict
lsm.manage
cgroup.manage
rlimit.manage
device.manage
bpf.manage
perf.open
kernel.sysctl
kernel.module
time.manage
rawio.access
power.manage
```

### 4.7 systemd·Privilege — 9개 Tool / 26개 Action

```text
systemd.unit_lifecycle
systemd.unit_enablement
systemd.trigger_unit
systemd.transient_run
systemd.unit_property
systemd.manager_reload
systemd.user_linger
systemd.scope_run
systemd.hostname_change
```

### 4.8 Docker·containerd·OCI — 16개 Tool / 47개 Action

```text
docker.container_create
docker.container_lifecycle
docker.exec
docker.copy
docker.resources_update
docker.restart_policy
docker.commit_export
docker.image_local
docker.volume_manage
docker.compose_local
docker.engine_local_request
containerd.task_manage
oci.runtime_run
oci.hook_run
cdi.device_inject
docker.log_manage
```

### 4.9 Persistence — 28개 Tool / 66개 Action

```text
persist.system_cron
persist.at_job
persist.systemd_unit
persist.systemd_trigger
persist.systemd_generator
persist.shell_profile
persist.ld_preload
persist.motd
persist.package_hook
persist.logrotate_hook
persist.udev_rule
persist.module_autoload
persist.initramfs_bootloader
persist.legacy_init
persist.binary_replace
persist.shell_rc
persist.user_cron
persist.user_systemd
persist.path_hijack
persist.tool_config
persist.environment
persist.setid_file
persist.filecap
persist.account_group
persist.sudoers
persist.tmpfiles
persist.sysusers
persist.sysctl
```

### 4.10 Audit·Evidence — 8개 Tool / 20개 Action

```text
audit.rule_manage
audit.lock
audit.user_record
audit.log_manage
audit.queue_pressure
journal.manage
login_record.manage
evidence.feedback
```

## 5. 현재 Tool 코드 계약

383개 `ToolDefinition`에는 다음 계약이 등록돼 있다.

- Action별 독립 `handler`
- Action별 독립 `verifier`
- Action별 독립 `resetter`
- `ToolSpec`
- 허용 Executor와 Trust Boundary
- Target과 `resource_ref` 종류
- 구조화된 인자 스키마
- timeout
- 파괴적 Action의 자원 제한과 비상 중단 조건
- rollback 실패 시 `RunGuard` 중단
- Evidence writer와 Harness abort callback 연결점

Tool 패키지는 백엔드 연동을 위해 다음 공개 API를 제공한다.

```python
get_definition(tool, action)
definition_manifest()
execute_tool_action(...)
```

## 6. 현재 미완료 사항

### 6.1 실제 Agent 실행 경로 연결

383개 `ToolDefinition`이 작성됐지만 현재 실제 Agent 실행 Runtime은 전체 Definition을 사용하지 않는다. 백엔드에서 실제 호출을 `execute_tool_action()`으로 연결해야 한다.

권장 호출 순서는 다음과 같다.

```text
Agent Tool Call
→ Harness/Supervisor 정책 및 resource manifest 준비
→ execute_tool_action()
→ ToolDefinition.handler
→ ToolDefinition.verifier
→ ToolDefinition.resetter
→ ToolExecution 반환
```

### 6.2 Legacy 레지스트리 제거

현재 다음 legacy 호환 레지스트리가 남아 있다.

```text
_REGISTRY
_VERIFIERS
_RESETS
dispatch()
verify()
reset()
```

최종적으로 제거해야 하지만 실제 Backend 호출과 테스트가 `execute_tool_action()`으로 전환되기 전에는 삭제하지 않는다.

안전한 제거 순서는 다음과 같다.

1. Backend가 `definition_manifest()`로 Tool 카탈로그를 읽는다.
2. 실제 Agent 호출을 `execute_tool_action()`으로 전환한다.
3. 테스트를 `ToolExecution` 기준으로 전환한다.
4. Host/Container 호출 테스트를 통과한다.
5. 저장소 전체 legacy 참조가 없는지 검사한다.
6. legacy registry와 decorator를 제거한다.

### 6.3 환경 검증

다음 환경 검증이 남아 있다.

- Ubuntu Linux Host
- U1 Host Executor
- C1 Container Executor
- U1/U2/C1/C2/C3 Target
- 8개 방향 Trust Boundary
- Docker Engine
- systemd
- namespace/capability/cgroup
- cron/account/Persistence fixture
- auditd/journald Evidence

Windows Native 환경에서는 Linux syscall, `/proc`, namespace, capability, systemd, auditd 실실행을 완료할 수 없다.

## 7. 판정 기준

| 상태 | 의미 |
|---|---|
| `DEFINED` | ToolDefinition 코드와 handler/verifier/resetter가 존재 |
| `STATIC_VERIFIED` | 등록·스키마·정책 계약 정적 검사 통과 |
| `LOCAL_FIXTURE_VERIFIED` | 적용 가능한 로컬 fixture 실행 통과 |
| `LINUX_VERIFIED` | Linux Host/Container 실제 syscall 실행 통과 |
| `DOCKER_VERIFIED` | Docker/containerd/OCI 실제 API 실행 통과 |
| `EC2_VERIFIED` | 격리 EC2에서 Evidence와 rollback까지 통과 |

`implemented_actions`에는 요구된 인증 단계까지 실제로 통과한 Action만 등록한다.

## 8. 최종 요약

현재 상태는 다음과 같다.

> **129개 Tool / 383개 Action의 ToolDefinition 코드는 작성됐고 각 Action의 handler/verifier/resetter 계약도 등록돼 있다. 실제 Agent에 노출된 범위는 5개 Tool / 20개 Action이며, 전체 Linux Host/Container 실실행 검증과 Backend 실행 경로 연결은 아직 완료되지 않았다.**

