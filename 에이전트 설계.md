# OS Agent 설계

## 1. 목적

OS Agent는 **하나의 EC2 내부에 Host 사용자와 Container로 구성된 고정 실험 인프라**를 대상으로 한다. 실험 시작 전에 Host와 Container의 권한 프로파일을 고정하고, 에이전트는 이 권한을 변경하지 않은 채 EC2 내부의 모든 Trust Boundary(TB)를 최악의 공격자 관점에서 시험한다.

실험의 최종 목적은 다음과 같다.

> 고정된 권한 프로파일로 EC2 내부 8개 TB 중 어느 경계가 실제로 뚫리는지, 뚫린 경계에서 도달 가능한 최고 위험 결과가 무엇인지 실행 증거로 검증한다.

```text
권한 프로파일 적용·검증
→ 에이전트 시작
→ 유효 권한·토폴로지 전달
→ Recon
→ 인프라 정보수집
→ 취약점 분석
→ 최고 위험 시나리오 계획
→ 도구 선택
→ 구조화된 명령 생성
→ 정책 검사
→ 실행
→ 증거 수집·검증·복구
```

여기서 인프라는 AWS 계층 전체가 아니라 **현재 실험용 EC2 한 대의 내부 구성 전체**를 뜻한다. 분석 대상은 Host OS, user1, user2, Runtime, Supervisor, Executor, Docker, C1/C2/C3, 내부 Network, Mount, Process, Service, Unix socket, Capability, Namespace, audit 및 로그다. IAM, VPC, Security Group, ECR, 다른 EC2 및 외부 시스템은 분석하지 않는다. SSM은 로컬 Dashboard와 EC2를 연결하는 전송 수단일 뿐 분석 대상이 아니다.

Canary는 파일 변조 위험을 검증하는 수단 중 하나일 뿐이다. 권한 상승, Container 경계, Docker 제어, Service 제어, Persistence 및 증거 훼손 가능성은 각각의 전용 Impact Verifier로 검증한다.

### 1.1 분석 범위와 실행 범위

- **분석 범위:** 실험용 EC2 한 대의 OS·사용자·Container·내부 Runtime 구성 전체
- **경로 비교 범위:** EC2 내부에 선언된 8개 Trust Boundary와 Action Path 전체
- **권한 조건:** 실험 시작 시 확정된 Host/Container 권한 프로파일을 종료까지 고정
- **시나리오 선택:** TB별 최고 위험 시나리오를 만든 뒤 8개 결과를 다시 비교
- **실행 범위:** EC2 내부의 등록된 실험 자원과 복구 가능한 fixture
- **영향 검증:** 위험 종류에 맞는 실제 syscall, process, network, service, Docker 및 파일 증거 사용
- **제외 범위:** AWS 제어 계층, 다른 EC2, 외부 인터넷 및 EC2 밖의 모든 시스템

### 1.2 실험 대상 TB

전체 실험은 다음 8개 경계를 동일한 고정 프로파일 조건에서 비교한다.

| TB | Source Executor | Target | Action Path |
|---|---|---|---|
| `TB-HH-U1U2` | U1 Host Executor | U2 | `U1U2` |
| `TB-HC-U1C1` | U1 Host Executor | C1 | `U1C1` |
| `TB-HC-U1C2` | U1 Host Executor | C2 | `U1C2` |
| `TB-HC-U1C3` | U1 Host Executor | C3 | `U1C3` |
| `TB-HC-C1U1` | C1 Container Executor | U1 | `C1U1` |
| `TB-HC-C1U2` | C1 Container Executor | U2 | `C1U2` |
| `TB-CC-C1C2` | C1 Container Executor | C2 | `C1C2` |
| `TB-CC-C1C3` | C1 Container Executor | C3 | `C1C3` |

Host 프로파일은 U1에서 시작하는 4개 TB에 적용하고, Container 프로파일은 C1에서 시작하는 4개 TB에 적용한다. 두 프로파일이 모두 고정되어야 8개 전체에 대한 최악의 경로를 비교할 수 있다.

## 2. 핵심 원칙

### 2.1 요청 권한과 유효 권한을 구분한다

에이전트에는 사용자가 요청한 `permission_profile`만 전달하지 않는다. Supervisor가 프로파일을 적용한 뒤 OS에서 직접 관측한 결과를 `effective_permissions`로 생성해 함께 전달한다.

```json
{
  "requested_permissions": {
    "mount_write": true,
    "supplementary_group": true,
    "docker_socket_access": false
  },
  "effective_permissions": {
    "uid": 10003,
    "gids": [10003, 10005],
    "mount_mode": "rw",
    "capabilities": [],
    "no_new_privileges": true,
    "docker_socket_accessible": false
  }
}
```

계획과 검증은 항상 `effective_permissions`를 기준으로 수행한다.

### 2.2 권한 프로파일은 실험 중 불변이다

실험 시작 시 Host와 Container 프로파일 전체를 정규화하고 `profile_hash`를 만든다. Recon, 분석, 계획 및 실행 단계는 모두 같은 hash를 사용해야 한다.

- 에이전트는 프로파일 값을 켜거나 끌 수 없다.
- 시나리오 성공을 위해 추가 권한을 요청할 수 없다.
- Supervisor는 각 Tool 실행 전에 현재 유효 권한이 시작 Snapshot과 일치하는지 확인한다.
- 임시 권한 전환 자체가 실험 Tool인 경우에도 고정 프로파일이 허용하는 범위 안에서만 시도한다.
- 프로파일이 달라지면 현재 Run을 중단하고 새로운 실험으로 기록한다.

에이전트가 바꿀 수 있는 것은 Recon 순서, 공격 시나리오, Tool 선택 및 명령 단계뿐이다.

### 2.3 모델은 임의 셸 명령을 직접 실행하지 않는다

모델은 다음과 같은 구조화된 행동만 생성한다.

```json
{
  "tool": "file.content",
  "action": "write",
  "resource_ref": "target-canary",
  "arguments": {
    "content": "os-agent-test"
  },
  "expected_result": "allowed",
  "reason": "RW mount와 Target GID가 모두 적용됨"
}
```

실제 OS 명령은 root 소유 Supervisor의 `Command Compiler`가 고정 allowlist와 검증된 인수를 이용해 생성한다. 모델이 작성한 문자열을 `shell=True`, `bash -c`, `sh -c` 등에 직접 전달하지 않는다.

### 2.4 실행과 판정을 분리한다

Executor는 실행 결과를 수집하고, Verifier는 별도로 다음 항목을 확인한다.

- 적용된 UID/GID 및 capability
- namespace와 mount 상태
- exit code와 errno
- 변경 전후 SHA-256
- auditd, journald, Docker event 증거
- 예상 결과와 실제 결과의 일치 여부
- 복구 완료 여부

## 3. 전체 구성

```text
Local Dashboard
  └─ Control Backend
      ├─ Permission Context Builder
      ├─ Agent Orchestrator
      ├─ Scenario Repository
      └─ SSM Tunnel Manager
             │
             ▼
EC2 내부 실험 Runtime
  ├─ Runtime API
  ├─ Recon Agent
  ├─ Infrastructure Collector
  ├─ Vulnerability Analyzer
  ├─ Risk Scenario Planner
  ├─ Tool Selector
  ├─ Policy Gate
  ├─ Host Supervisor (root, allowlist-only)
  ├─ U1 Host Executor (user1)
  ├─ C1 Container Executor
  ├─ U2/C2/C3 Targets
  └─ Verifier / Evidence Collector / Rollback
```

## 4. 단계별 설계

### 4.1 권한 프로파일 적용

입력:

- `run_id`
- 전체 `host_permission_profile`
- 전체 `container_permission_profile`
- 고정 토폴로지와 8개 `trust_boundary_ids`
- 서버에 고정된 공격 Agent 임무
- 진행 관찰 구간과 비정상 루프 방지 Watchdog

처리:

1. Host/Container 프로파일의 모든 키와 boolean 값을 검증한다.
2. 각 프로파일을 정규화해 실험 전체의 `profile_hash`를 만든다.
3. Supervisor가 U1 또는 C1 실행 문맥을 만들 때 해당 고정 프로파일을 적용한다.
4. 실제 UID/GID, capability, mount, namespace 및 socket 접근성을 관측한다.
5. 요청 상태와 실제 상태가 다르면 Agent 실행 전에 중단한다.
6. 각 TB 실행 직전과 직후에 `profile_hash`와 유효 권한 Snapshot을 재검증한다.

출력:

- `requested_permissions`
- `effective_permissions`
- `profile_hash`
- `profile_application_checks`
- `profile_warnings`

### 4.2 에이전트 시작 및 Context 주입

에이전트 시작 시 다음 Context를 읽기 전용으로 전달한다.

- 서버에 고정된 공격 임무(사용자 Prompt 입력 없음)
- 고정 Host/Container 요청 권한과 실제 유효 권한
- 변경 불가능한 `profile_hash`
- 8개 Trust Boundary와 source/target 노드
- 등록된 Tool과 action 목록
- 등록된 `resource_ref`
- 진행 관찰 구간과 실행 시간·Tool 호출 Watchdog
- 금지 행위와 복구 요구사항

에이전트가 권한 프로파일 자체를 변경할 수 없도록 한다. 권한 변경이 필요한 새 시나리오는 현재 Run을 종료하고 별도 Run으로 생성한다. 에이전트는 한 TB만 선택해서 끝내지 않고 8개 TB 각각에 대한 후보와 판정을 생성해야 한다.

### 4.3 Recon

Recon은 공격 실행 전에 현재 실행 문맥을 확인하는 읽기 전용 단계다.

수집 대상:

- 실제 UID, EUID, GID, supplementary groups
- effective/bounding/ambient capability
- `no_new_privs`, seccomp, AppArmor 상태
- PID, IPC, mount, network namespace
- bind mount 목록과 RO/RW 상태
- 등록 Target 파일의 메타데이터
- 허용된 내부 네트워크와 등록 서비스 연결성
- Docker socket 존재 및 접근 가능 여부

Recon Tool은 읽기 전용 allowlist만 사용하며 결과를 `ReconSnapshot`으로 정규화한다.

Recon은 한 개 Target만 조사하지 않는다. EC2 내부 자산 목록을 만든 뒤 8개 Trust Boundary와 Action Path별 접근 가능성, 권한 전이 조건 및 관측 가능한 방어 통제를 조사한다.

### 4.4 인프라 정보수집

Recon 결과와 고정 토폴로지를 결합해 현재 Run에서 접근 가능한 경로만 구성한다.

예시:

```json
{
  "source": "c1",
  "reachable_targets": ["c2"],
  "action_paths": ["C1C2"],
  "mounts": [
    {
      "resource_ref": "target-canary",
      "mode": "rw",
      "owner_uid": 0,
      "group_gid": 10005,
      "mode_bits": "0660"
    }
  ]
}
```

SSM은 로컬 연결에만 사용하고 에이전트 Recon 대상에서 제외한다. EC2 내부 OS 및 Docker 정보수집은 명시적으로 등록된 읽기 전용 Tool만 허용한다. AWS API는 호출하지 않는다.

### 4.5 취약점 분석

Vulnerability Analyzer는 관측 사실과 고정 권한의 조합으로 **TB별** 후보를 생성한다. 단일 권한만 보고 성공을 단정하지 않으며, 프로파일에 없는 권한이 필요한 후보는 실행 가능 시나리오에서 제외한다.

예시 규칙:

- `mount_write=ON`이어도 파일 DAC가 막으면 쓰기 불가
- `mount_write=ON`과 Target GID 보유가 동시에 확인되면 변조 가능성 상승
- `limited_sudo=ON`이어도 `no_new_privs=ON`이면 setuid 전환 실패 가능성
- Docker socket이 mount되어도 socket DAC 또는 group이 없으면 접근 불가
- root UID여도 capability, namespace, LSM 제한이 남아 있을 수 있음

취약점 후보 형식:

```json
{
  "finding_id": "finding-001",
  "trust_boundary_id": "TB-CC-C1C2",
  "title": "C1에서 C2 Canary 변조 가능",
  "preconditions": [
    "target mount is rw",
    "effective groups contains 10005"
  ],
  "impact": "target_data_modification",
  "confidence": 0.95,
  "evidence_refs": ["recon:mounts:0", "recon:identity"]
}
```

### 4.6 위험도 기반 계획

Risk Scenario Planner는 각 TB에서 고정 권한으로 실행 가능한 시나리오를 만들고 다음 기준으로 평가한다.

```text
risk_score = impact × success_probability × evidence_strength
             - uncertainty_penalty - rollback_penalty
```

권장 등급:

- 90~100: Critical
- 70~89: High
- 40~69: Medium
- 1~39: Low

계획 절차:

- 각 TB에 대해 가능한 최고 위험 시나리오를 최소 1개 생성
- Source Executor에 대응하는 고정 권한 프로파일만 사용
- 프로파일에 없는 권한을 가정하거나 주입하지 않음
- 8개 Action Path의 후보와 위험도를 같은 기준으로 비교
- 등록된 Tool과 resource만 사용
- 위험 종류에 맞는 Impact Verifier가 존재
- 자동 복구 또는 상태 초기화 가능
- 증거로 성공 여부를 판정할 수 있음

계획은 처음부터 고정된 Tool 목록이 아니다. Agent는 최신 구조화 결과와 현재
state fingerprint를 입력으로 받아 다음 최적 Tool 하나를 고르고, 실행 결과를
다시 관찰해 다음 단계 또는 의미 기반 종료를 정한다. 같은 TB 안에서는 Tool 사이에
fixture를 초기화하지 않으므로 여러 Tool의 상태와 영향이 누적된다.

```json
{
  "scenario_id": "scenario-001",
  "trust_boundary_id": "TB-CC-C1C2",
  "risk_level": "high",
  "objective": "C2 Target 데이터 변조 가능성 검증",
  "chain_id": "chain-os-...-tb-cc-c1c2",
  "steps": [],
  "search": {
    "status": "PENDING",
    "frontier_candidates": 20,
    "termination_reason": null,
    "resume_available": false
  }
}
```

`steps`에는 Agent가 실제 선택·실행한 Tool만 순서대로 추가한다. 각 단계는 선택 이유,
Policy/Runtime/Verifier 결과, 실행 전후 state fingerprint와 상태 변화 증거를 기록한다.

TB별 계획이 모두 완료되면 Planner는 다음 두 결과를 별도로 만든다.

1. `tb_scenarios`: 8개 TB 각각의 최고 위험 시나리오
2. `worst_case_scenario`: 실제로 `BROKEN` 판정을 받은 TB 중 위험 점수가 가장 높은 시나리오

분석 점수가 높아도 실제 검증에 실패하면 최종 최악 경로로 확정하지 않는다.

### 4.7 도구 선택

Tool Selector는 각 계획 단계에 대해 다음을 검사한다.

- 해당 Tool/action이 구현되어 있는가
- 현재 Executor에서 사용할 수 있는가
- 현재 시험 중인 Trust Boundary의 source/target과 맞는가
- 필요한 권한이 실제로 적용됐는가
- Tool 실행이 고정 `profile_hash`를 변경하지 않는가
- 변경 범위와 복구 방식이 정의됐는가

구현되지 않은 Tool은 계획 후보에서 제외하며, 모델이 이름이 비슷한 Tool을 임의 생성할 수 없게 enum으로 제한한다.

### 4.8 구조화된 명령 생성

Command Compiler는 구조화된 Tool Call을 고정 실행 명령으로 변환한다.

```text
Model output
  file.content/write/target-canary
        ↓
Policy Gate
        ↓
Command Compiler
        ↓
고정 Python Runtime Agent 또는 고정 argv 배열
```

필수 검증:

- 문자열 길이 제한
- NUL 및 제어문자 거부
- 경로를 `resource_ref`에서만 해석
- `..`, symlink 탈출, 절대경로 직접 입력 거부
- 환경변수 및 shell expansion 금지
- 실행 파일 경로 고정
- timeout, pids, memory 및 output 크기 제한

### 4.9 Policy Gate

실행 직전 다음 조건을 모두 만족해야 한다.

- Run과 Tool Call의 `subject_mode` 일치
- 현재 시험 중인 Trust Boundary의 source/target 일치
- 시작 시 고정된 `profile_hash`와 현재 권한 Snapshot 일치
- Tool/action/resource 조합이 allowlist에 존재
- 현재 단계가 승인된 시나리오에 포함
- 실행 대상이 현재 EC2 내부에 등록된 실험 자원
- 선택한 위험 종류에 대응하는 Impact Verifier와 rollback 정의 존재
- 비정상 루프 방지 Watchdog 상태 확인
- rollback 정의 존재

하나라도 실패하면 해당 Tool을 `POLICY_BLOCKED`로 기록하고 실행하지 않은 뒤 TB 전체를
긴급 복구한다. 아직 frontier가 남았다면 경계 전체를 `BLOCKED`로 확정하지 않고
`INCONCLUSIVE`로 기록한다.

### 4.10 실행, 검증 및 복구

각 Tool 단계의 상태는 다음과 같다.

```text
PENDING → POLICY_CHECKED → DISPATCHED → EXECUTED → VERIFIED
```

Tool 사이에는 rollback하지 않는다. TB 체인이 의미 기반 종료되거나 오류·취소·Watchdog으로
중단되면 `finally` 경로에서 시나리오 전체 rollback을 정확히 한 번 수행한다.

오류 상태:

```text
POLICY_BLOCKED | OS_DENIED | EXECUTION_ERROR |
VERIFICATION_FAILED | ROLLBACK_FAILED | BUDGET_EXCEEDED
```

OS가 거부한 결과는 시스템 오류가 아니다. 계획의 예상 결과가 `denied`였고 실제로 errno 13 등으로 거부됐다면 실험 결과는 `PASS`가 될 수 있다.

### 4.11 TB 판정

각 TB는 다음 중 하나로 판정한다.

- `BROKEN`: 고정 권한만으로 Source가 경계를 넘어 Target의 기밀성·무결성·가용성·실행 제어 중 하나에 실제 영향을 만들었고 전용 Verifier가 이를 확인함
- `BLOCKED`: 구현·실행 가능한 공격 frontier가 실제 OS 통제로 소진됐고 차단 증거가 존재함
- `INCONCLUSIVE`: Tool 미구현, 증거 부족, 예산 초과 또는 복구 실패로 경계 상태를 확정할 수 없음

`BROKEN`은 반드시 보안 설정 오류만을 의미하지 않는다. 고정 프로파일이 의도적으로 부여한 권한 때문에 경계를 넘을 수 있는 경우도 포함하며, 이는 해당 프로파일의 실제 blast radius로 보고한다.

TB별 필수 결과:

| 필드 | 의미 |
|---|---|
| `trust_boundary_id` | 시험한 TB |
| `verdict` | `BROKEN`, `BLOCKED`, `INCONCLUSIVE` |
| `highest_impact` | 해당 TB에서 검증된 최고 영향 |
| `attack_path` | Recon부터 영향 발생까지의 단계 |
| `fixed_permissions_used` | 성공 또는 차단에 관여한 고정 권한 |
| `effective_identity` | 실제 UID/GID/capability/namespace |
| `risk_score` | 영향도·성공 가능성·증거 강도 기반 점수 |
| `proof_level` | 실제 검증 강도 |
| `evidence_refs` | syscall, hash, audit, process 및 service 증거 |
| `rollback_status` | 복구 결과 |

### 4.12 위험 종류별 Impact Verifier

Canary 하나로 모든 위험을 판정하지 않는다.

| 위험 영역 | 실제 검증 방식 |
|---|---|
| 파일·Mount | 전용 파일의 read/write/execute, metadata 및 전후 hash 비교 |
| UID/GID 전환 | 격리 자식 프로세스에서 실제 `setuid`/`setgid` 계열 syscall과 도달 신분 확인 |
| Capability | 전용 Probe에서 capability가 필요한 kernel 동작을 실행하고 errno 확인 |
| Process·Service | 등록된 실험 service의 상태 조회 및 복구 가능한 제어 결과 확인 |
| Container 경계 | C1/C2/C3 내부의 전용 fixture에 실제 접근하고 namespace·mount 증거 확인 |
| Docker 제어 | 폐기 가능한 Probe Container의 생성·조회·종료 가능성 확인 |
| 내부 Network | EC2 내부 등록 endpoint 연결, 인증 및 요청 결과 확인 |
| Persistence | 실험 전용 systemd/cron fixture의 설치·발동·제거와 잔여 상태 확인 |
| 증거 훼손 | 전용 evidence fixture의 변경 가능성과 audit 탐지 여부 확인 |
| Host 장악 | UID, capability, Host namespace, Host filesystem 및 service 제어 증거를 조합해 판정 |

검증 강도:

```text
L0 INFERRED   설정과 Recon만으로 추론
L1 REACHABLE  Target 도달 또는 객체 open 성공
L2 EXECUTED   실제 syscall/API/Tool 동작 수행
L3 IMPACTED   Target 상태나 실행 제어에 실제 영향 발생
L4 RESTORED   영향 검증 후 원상 복구까지 확인
```

`BROKEN` 확정에는 원칙적으로 L3 이상의 증거가 필요하다. L0~L2만 확보되면 `INCONCLUSIVE` 또는 제한된 위험으로 표시한다.

## 5. 에이전트 반복 루프

```text
1. 고정 프로파일 적용·검증
2. EC2 내부 전체 RECON
3. 8개 TB별 ANALYZE·PLAN
4. TB별 POLICY_CHECK·ACT·VERIFY
5. TB별 BROKEN/BLOCKED/INCONCLUSIVE 판정
6. TB별 ROLLBACK
7. 8개 결과 위험도 비교
8. 실제 검증된 WORST_CASE 확정
```

진행/장애 Watchdog 기본값:

- 진행 재평가 soft window: 16 Tool 단계(새 상태가 있으면 자동 연장)
- TB별 Tool Watchdog: 64회
- TB별 시간 Watchdog: 600초
- 동일 상태·전이 없는 Planner 판단: 4회
- TB별 최대 변경 Target: 1개
- 최대 출력: Tool당 64KiB

이 값은 보안상 안전을 뜻하는 탐색 한도가 아니다. 최종 영향 검증, 실행 가능한 frontier
소진, 중복 상태·전이 소진처럼 의미 있는 조건으로 종료한다. Watchdog이 먼저 작동하면
반드시 `INCONCLUSIVE`와 `SEARCH_BUDGET_EXHAUSTED` 또는 `WATCHDOG_TIMEOUT`으로 기록하고
전체 복구를 검증한 뒤 replay용 checkpoint를 저장한다. `BLOCKED`나 안전 판정으로 바꾸지 않는다.

## 6. Run 데이터 모델

기존 Run 모델에 다음 필드를 추가한다.

```json
{
  "agent_stage": "recon",
  "fixed_permission_profiles": {
    "host": {},
    "container": {}
  },
  "profile_hash": "sha256:...",
  "effective_permissions": {
    "host": {},
    "container": {}
  },
  "recon_snapshot": {},
  "infrastructure_snapshot": {},
  "findings": [],
  "tb_results": [],
  "tb_scenarios": [{
    "chain_id": "chain-os-...",
    "steps": [],
    "search": {"status": "RUNNING", "checkpoint": {}}
  }],
  "worst_case_scenario": {},
  "budget": {},
  "rollback_status": "NOT_REQUIRED"
}
```

대용량 Recon과 Evidence는 별도 테이블 또는 객체로 저장하고 Run에는 참조 ID만 저장할 수 있다.

## 7. 이벤트 모델

최소 이벤트 유형:

- `PROFILE_REQUESTED`
- `PROFILE_APPLIED`
- `PROFILE_VERIFIED`
- `PROFILE_HASH_LOCKED`
- `AGENT_STARTED`
- `RECON_STARTED`
- `RECON_OBSERVED`
- `INFRASTRUCTURE_COLLECTED`
- `FINDING_CREATED`
- `SCENARIO_CANDIDATE_CREATED`
- `SCENARIO_SELECTED`
- `TB_TEST_STARTED`
- `PLAN_CREATED`
- `TOOL_SELECTED`
- `POLICY_ALLOWED`
- `POLICY_BLOCKED`
- `COMMAND_COMPILED`
- `RUNTIME_DISPATCHED`
- `TOOL_RESULT`
- `STEP_VERIFIED`
- `TB_VERDICT_RECORDED`
- `ROLLBACK_STARTED`
- `ROLLBACK_VERIFIED`
- `WORST_CASE_SELECTED`
- `RUN_FINISHED`

각 이벤트에는 `sequence`, `stage`, `source`, `message`, `payload`, `created_at`을 포함한다.

## 8. Executor 분리

### U1 Host Executor

- 실제 실행 UID: user1 고정 UID
- user2 및 C1/C2/C3 경계 실험 담당
- Docker 그룹은 기본적으로 미가입
- 필요한 supplementary group과 capability는 해당 프로세스에만 적용
- root 작업은 고정 sudo helper가 허용된 경우에만 가능

### C1 Container Executor

- Docker socket 직접 접근 금지
- Supervisor Unix socket을 통해서만 실행 요청
- C2와 C3는 서로 다른 internal network 사용
- Target에 따라 `os-agent-c1-c2` 또는 `os-agent-c1-c3` 선택
- Host Target 실험에는 불필요한 Docker network를 연결하지 않음

## 9. 안전 경계

다음 행위는 기본 차단한다.

- 현재 EC2 밖의 IP, hostname 및 시스템 접근
- 외부 인터넷 대상 Recon
- 임의 셸 또는 임의 바이너리 실행
- 실제 사용자 데이터 변경
- IAM, VPC, Security Group, ECR 및 AWS 제어 계층 조사·변경
- 다른 EC2 또는 다른 팀 환경 접근
- 무제한 Docker API 호출
- 실험 fixture 밖의 영구 persistence 생성
- 복구 정의가 없는 파괴적 행동
- 실제 증거 로그 삭제 또는 변조

에이전트는 현재 EC2 내부 전체와 8개 TB를 조사해 최고 위험 시나리오를 찾아야 한다. 검증은 파일 Canary에만 의존하지 않고 위험 종류별 Impact Verifier를 사용한다. 실제 OS·Container 동작은 수행하되 원본 데이터를 파괴하는 대신 복구 가능한 전용 실험 자원에서 L3 영향과 L4 복구를 입증한다.

## 10. API 제안

```text
POST /api/remote/agent-runs
GET  /api/remote/agent-runs/{run_id}
GET  /api/remote/agent-runs/{run_id}/events
GET  /api/remote/agent-runs/{run_id}/recon
GET  /api/remote/agent-runs/{run_id}/findings
GET  /api/remote/agent-runs/{run_id}/plan
POST /api/remote/agent-runs/{run_id}/cancel
POST /api/remote/agent-runs/{run_id}/rollback
```

Run 시작 요청:

```json
{
  "prompt": "고정 권한으로 8개 TB를 모두 시험하고 최악의 침해 경로를 검증해줘",
  "scope": "all_trust_boundaries",
  "fixed_permission_profiles": {
    "host": {
      "owner_write": false,
      "group_write": false,
      "limited_sudo": false,
      "no_new_privileges": true,
      "dac_override": false,
      "setuid_capability": false,
      "setgid_capability": false,
      "sys_ptrace_capability": false,
      "docker_group_access": false
    },
    "container": {
      "mount_write": false,
      "run_as_root": false,
      "supplementary_group": false,
      "dac_override": false,
      "setuid_capability": false,
      "setgid_capability": false,
      "sys_ptrace_capability": false,
      "no_new_privileges": true,
      "pid_namespace_host": false,
      "ipc_namespace_host": false,
      "apparmor_unconfined": false,
      "seccomp_unconfined": false,
      "systempaths_unconfined": false,
      "privileged": false,
      "docker_socket_access": false
    }
  },
  "planner_model": "configured-model",
  "budget": {
    "max_steps_per_tb": 8,
    "max_tool_calls_per_tb": 12,
    "max_elapsed_seconds_per_tb": 60
  }
}
```

최종 응답은 8개 TB 결과와 실제 검증된 최악 경로를 함께 반환한다.

```json
{
  "profile_hash": "sha256:...",
  "summary": {
    "broken": 2,
    "blocked": 5,
    "inconclusive": 1
  },
  "tb_results": [
    {
      "trust_boundary_id": "TB-CC-C1C2",
      "verdict": "BROKEN",
      "highest_impact": "target_data_modification",
      "risk_score": 82,
      "proof_level": "L4_RESTORED"
    }
  ],
  "worst_case_scenario": {
    "trust_boundary_id": "TB-HC-C1U1",
    "risk_level": "critical",
    "objective": "container_to_host_control"
  }
}
```

## 11. 구현 순서

1. `AgentRunRecord`, stage 및 event schema 추가
2. Permission Context Builder에서 유효 권한 Snapshot 생성
3. 읽기 전용 Recon Tool 구현
4. Infrastructure Snapshot 정규화
5. 8개 TB별 규칙 기반 Vulnerability Analyzer 구현
6. 고정 프로파일 기반 TB별 Scenario Planner와 전체 비교기 구현
7. 구조화된 Tool Plan schema와 validator 추가
8. Policy Gate 및 Command Compiler 분리
9. 다단계 Agent Orchestrator 구현
10. 위험 종류별 Impact Verifier와 Rollback 구현
11. Dashboard에 8개 TB 판정표와 최악 경로 표시
12. C1 및 user1에서 8개 Action Path E2E 회귀 테스트 추가

규칙 기반 분석과 고정 Planner를 먼저 구현한 후, 같은 구조화된 입력·출력 계약 위에 LLM Planner를 연결한다. 이렇게 해야 모델이 없어도 재현 가능한 테스트가 유지된다.

## 12. 완료 기준

- 완전한 Host/Container 권한 프로파일이 실험 시작 시 한 번 고정된다.
- 전체 Run과 모든 TB 이벤트에 같은 `profile_hash`가 기록된다.
- 권한 적용 결과와 실제 유효 권한이 일치하고 TB 실행 전후에도 변하지 않는다.
- C1과 user1 Executor가 서로 분리된 실행 신분으로 동작한다.
- Recon이 EC2 내부 전체를 대상으로 실행되고 증거 참조가 남는다.
- 8개 TB 모두에 관측 사실 기반 취약점 후보와 최고 위험 계획이 생성된다.
- 8개 TB가 각각 `BROKEN`, `BLOCKED`, `INCONCLUSIVE` 중 하나로 판정된다.
- `BROKEN` 판정은 위험 종류별 Impact Verifier의 L3 이상 실제 증거를 가진다.
- Planner가 실제로 검증된 `BROKEN` 결과 중 최고 위험 경로를 최종 선택한다.
- 모델 출력에는 임의 raw shell command가 없다.
- 모든 명령이 Policy Gate와 Command Compiler를 통과한다.
- 에이전트가 실험 도중 권한 프로파일을 변경하거나 추가 권한을 요청하지 않는다.
- C2와 C3가 동일 네트워크를 공유하지 않는다.
- 파일뿐 아니라 UID/GID, capability, process, service, Docker, network, persistence 및 증거 위험에 맞는 검증기가 사용된다.
- 실행 후 Canary, fixture, 임시 process/service/container 및 권한 상태가 원상 복구된다.
- AWS API, IAM, VPC, ECR, 다른 EC2 및 외부 시스템은 분석·계획·실행에 포함되지 않는다.
- 실패 시에도 500으로 뭉개지 않고 단계·원인·증거가 반환된다.
- 같은 입력과 환경에서 규칙 기반 실행 결과를 재현할 수 있다.
