# OS Agent 실행 방법

Windows 팀원은 Git만 설치되어 있다고 가정한다. 최초 환경설정은 `setup.cmd`, 평소 실행은 `run.cmd`를 사용한다. PowerShell 명령을 직접 입력할 필요는 없다.

## 1. 처음 설치하는 팀원

### 1-1. 저장소 받기

CMD에서 저장소를 clone하고 프로젝트 폴더로 이동한다.

```cmd
git clone https://github.com/whs-agentic-ai-lab/OS-Agent.git os-Agent-test
cd os-Agent-test
```

이미 clone한 저장소라면 다음 명령으로 최신 코드를 받는다.

```cmd
git pull
```

### 1-2. `backend/.env` 받기

프로젝트 관리자에게 `.env` 파일을 받아 다음 경로에 넣는다.

```text
os-Agent-test\backend\.env
```

`backend/.env`가 없으면 `run.cmd`는 실행을 중단하고 파일을 받아 넣으라고 안내한다. 실제 API 키가 들어 있는 `.env`는 Git에 올리거나 메신저 공개 채널에 붙여 넣지 않는다.

### 1-3. 최초 환경설정 실행

프로젝트 최상위 폴더에서 다음 파일을 실행한다.

```cmd
setup.cmd
```

파일을 더블 클릭해도 된다. `setup.cmd`는 다음 프로그램을 확인하고, 없거나 버전이 낮으면 `winget`으로 설치한다.

- Python 3.10 이상
- Node.js 22 이상과 npm
- AWS CLI v2
- Terraform 1.6 이상
- Docker Desktop
- AWS Session Manager Plugin

이미 설치된 항목은 자동으로 건너뛰므로 `setup.cmd`를 다시 실행해도 된다. 설치 여부만 확인하며 Python 패키지와 프론트엔드 패키지는 이후 `run.cmd`가 동기화한다.

설치 중 다음 단계는 Windows 화면 안내를 직접 완료해야 한다.

1. 관리자 권한 확인 창이 나오면 허용한다.
2. Docker Desktop 약관이 나오면 동의한다.
3. WSL 업데이트나 Windows 재부팅이 필요하면 안내대로 진행한다.
4. Docker Desktop에 `Engine running`이 표시될 때까지 기다린다.
5. CMD에 완료 여부를 묻는 메시지가 나오면 완료 후 `Y`를 입력한다.

재부팅했다면 프로젝트 폴더에서 `setup.cmd`를 다시 실행한다. 완료된 항목은 건너뛰고 남은 단계부터 이어진다. 마지막에 `지금 OS Agent를 실행할까요?`가 나오면 `Y`를 눌러 바로 실행할 수 있다.

`winget`이 없다는 안내가 나오면 Microsoft Store에서 `앱 설치 관리자(App Installer)`를 설치하거나 업데이트한 뒤 `setup.cmd`를 다시 실행한다.

## 2. 평소 실행

프로젝트 최상위 폴더에서 다음 파일만 실행한다.

```cmd
run.cmd
```

파일을 더블 클릭해도 된다. `run.cmd`는 다음 작업을 자동으로 수행한다.

1. Python과 Node.js 확인
2. Terraform, Docker Desktop과 AWS CLI 확인
3. Docker Desktop이 꺼져 있으면 실행하고 Engine 준비 대기
4. `whs-team` AWS 로그인 상태 확인
5. `backend/.env` 로드
6. Python 가상환경 생성 또는 재사용
7. 변경된 백엔드·프론트엔드 의존성만 설치
8. 로컬 백엔드와 프론트엔드 실행
9. 백엔드 헬스 체크 후 `http://127.0.0.1:5173` 자동 열기

Python 가상환경이 이미 있고 `requirements.txt`가 바뀌지 않았다면 백엔드 설치를 건너뛴다. `node_modules`가 있고 `package-lock.json`이 바뀌지 않았다면 프론트엔드 설치도 건너뛴다. 따라서 `git pull` 후에도 우선 `run.cmd`를 실행하면 된다.

백엔드는 `127.0.0.1:8000`, 프론트엔드는 `127.0.0.1:5173`을 사용한다. 해당 포트의 기존 OS Agent가 실행 중이면 새로 띄우지 않고 기존 프로세스를 사용한다.

### AWS 로그인 창이 나오는 경우

AWS 로그인이 없거나 세션이 만료되면 `run.cmd`가 확인 창을 띄운다. `확인`을 누르면 브라우저 로그인이 시작된다.

- SSO 설정 프로필이면 `aws sso login --profile whs-team`을 사용한다.
- 일반 로그인 프로필이면 `aws login --profile whs-team --region us-east-1`을 사용한다.
- 브라우저에 자격 증명 공유 성공 메시지가 나오면 해당 탭을 닫고 CMD가 계속 진행될 때까지 기다린다.

로그인 성공 여부는 다음 명령과 같은 방식으로 다시 검증된다.

```cmd
aws sts get-caller-identity --profile whs-team --region us-east-1
```

브라우저 로그인은 AWS 인증만 수행한다. 팀원에게 부여되는 실제 권한은 로그인한 AWS IAM/SSO 계정의 권한 정책을 따른다.

### AWS 없이 로컬 화면만 실행

Terraform 배포와 AWS 로그인이 필요 없는 로컬 UI·API 테스트라면 다음처럼 실행할 수 있다.

```cmd
run.cmd -SkipAwsLogin
```

이 모드는 AWS 로그인, Terraform 및 Docker 준비 확인을 건너뛴다. AWS 배포, EC2 목록과 SSM 연결은 사용할 수 없거나 준비되지 않은 상태로 표시될 수 있다.

## 3. AWS 환경 만들기

대시보드의 `환경 이름`에 3~16자의 영문 소문자, 숫자, 하이픈을 입력한다.

```text
permission-test
```

로그인한 IAM/SSO 사용자 정보와 입력한 이름을 조합해 고유 환경 ID를 만든다.

```text
os-agent-test-{사용자}-{짧은 식별값}-{환경 이름}
```

예시:

```text
os-agent-test-vinny-a1b2c3-permission-test
```

`AWS 환경 배포`를 누르고 비용 발생 확인 창을 승인하면 다음 작업을 자동으로 수행한다.

1. Terraform 초기화
2. 환경 전용 ECR 생성
3. 백엔드 Docker 이미지 build와 ECR push
4. VPC, NAT Gateway, IAM, EC2 등 고정 인프라 생성
5. EC2에서 백엔드 실행

Region/AZ는 `us-east-1 / us-east-1a`, EC2는 `t3.small` 1대로 고정된다. EC2의 `Name`, `EnvironmentId`, `CreatedBy`, `OwnerArn` 태그에 환경과 생성자 정보가 기록된다.

이 작업은 EC2뿐 아니라 NAT Gateway, EBS, VPC Endpoint, ECR과 CloudWatch Logs 등 비용이 발생할 수 있는 리소스를 생성한다.

## 4. EC2 목록과 SSM 연결

대시보드는 로컬 메모리가 아니라 실제 AWS 계정에서 프로젝트 EC2 목록을 주기적으로 다시 조회한다. 대시보드를 껐다 켜거나 팀원이 EC2를 여러 개 만들어도 목록에서 확인할 수 있다.

목록에는 다음 정보가 표시된다.

- 환경 ID와 EC2 Name
- 생성한 IAM/SSO 사용자
- 인스턴스 ID와 유형
- EC2 실행 상태
- SSM 연결 상태

연결할 EC2의 라디오 버튼을 선택한 뒤 `선택 EC2 SSM 연결`을 누른다. SSM 관리 노드가 `Online`이 되면 다음 터널이 열린다.

```text
127.0.0.1:8001 → 선택한 EC2:8000
```

로컬 FastAPI는 계속 8000 포트를 사용하고, AWS 백엔드 요청만 8001 터널로 전달된다. 이미 다른 EC2에 연결되어 있다면 먼저 `SSM 연결 종료`를 누른 뒤 새 EC2를 선택한다.

## 5. EC2 종료와 환경 전체 삭제 차이

### EC2만 종료

`EC2만 종료`는 선택한 인스턴스에 AWS terminate 요청을 보낸다. 확인을 위해 인스턴스 ID를 정확히 다시 입력해야 한다.

이 작업은 EC2만 종료하며 NAT Gateway, ECR, VPC Endpoint, CloudWatch Logs 같은 다른 리소스는 남는다. 따라서 전체 비용이 중단되는 것은 아니다. 테스트 환경 전체가 필요 없으면 가능한 한 `환경 전체 삭제`를 사용한다.

### 환경 전체 삭제

`환경 전체 삭제`는 Terraform이 관리하는 EC2, NAT Gateway, ECR과 네트워크 리소스를 함께 삭제한다. 확인을 위해 전체 환경 ID를 정확히 다시 입력해야 한다.

각 환경의 Terraform state는 환경을 만든 PC의 다음 경로에 따로 저장된다.

```text
backend\runtime\terraform-states\{환경 ID}\terraform.tfstate
```

따라서 다음 제한이 있다.

- 환경을 만든 PC: `환경 전체 삭제` 가능
- 다른 팀원의 PC: 로컬 state가 없으므로 `환경 전체 삭제` 불가능
- 다른 팀원의 PC에서도 권한이 있으면 `EC2만 종료`는 가능

Terraform state 파일을 임의로 삭제하거나 Git에 올리지 않는다. 다른 PC에서 환경 전체를 관리해야 한다면 state 이전 또는 원격 Terraform backend 도입이 별도로 필요하다.

선택한 EC2에 SSM 터널이 연결되어 있거나 인프라 작업이 진행 중이면 종료·삭제 버튼이 비활성화된다. 먼저 터널을 종료하고 진행 중인 작업이 끝날 때까지 기다린다.

## 6. 종료 후 다시 실행

로컬 프로그램만 끄려면 실행된 백엔드·프론트엔드 터미널에서 `Ctrl+C`를 누르거나 해당 창을 닫는다. 로컬 프로그램을 종료해도 AWS 리소스는 계속 실행되고 비용이 발생할 수 있다.

다음 실행부터는 프로젝트 폴더에서 다시 실행한다.

```cmd
run.cmd
```

## 7. 자주 발생하는 오류

### `backend/.env 파일이 없습니다`

관리자에게 파일을 받아 `os-Agent-test\backend\.env`에 넣고 `run.cmd`를 다시 실행한다.

### AWS 브라우저 로그인은 성공했는데 런처가 실패함

CMD에서 다음 명령을 확인한다.

```cmd
aws --version
aws sts get-caller-identity --profile whs-team --region us-east-1
```

두 번째 명령이 실패하면 `whs-team` 프로필 설정 또는 AWS CLI 버전을 확인하고 다시 로그인한다.

### `npm error code EPERM` 또는 `rolldown-binding...node` 잠금 오류

`run.cmd`는 이 프로젝트의 기존 Vite 프로세스를 종료하고 최대 두 번 다시 설치한다. 그래도 실패하면 다음을 확인한다.

1. 실행 중인 OS Agent 프론트엔드와 Node/Vite 터미널을 종료한다.
2. 해당 프로젝트를 검사 중인 백신·보안 프로그램이 파일을 잠그고 있지 않은지 확인한다.
3. `run.cmd`를 다시 실행한다.

관리자 CMD로 실행하는 것은 마지막 수단이며, 일반 실행으로 먼저 해결한다.

### Docker Engine이 준비되지 않음

Docker Desktop을 직접 열고 약관, WSL 업데이트와 재부팅 안내를 완료한다. `Engine running`을 확인한 뒤 `setup.cmd` 또는 `run.cmd`를 다시 실행한다.

### 8000 또는 5173 포트 오류

기존 OS Agent가 아닌 다른 프로그램이 포트를 사용 중이면 해당 프로그램을 종료한 뒤 `run.cmd`를 다시 실행한다.

### `Terraform state가 없습니다`

선택한 환경을 현재 PC에서 만들지 않았다는 뜻이다. 환경을 만든 팀원의 PC에서 전체 삭제하거나, 비용 경고를 확인한 뒤 EC2만 종료한다.

## 8. 개발자 수동 실행과 검증

평소에는 `run.cmd`를 사용한다. 문제를 분리해서 확인해야 할 때만 각각 실행한다.

백엔드:

```cmd
cd backend
.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

프론트엔드:

```cmd
cd frontend
npm run dev -- --host 127.0.0.1
```

백엔드 테스트:

```cmd
cd backend
.venv\Scripts\python.exe -m pytest -q
```

프론트엔드 검증:

```cmd
cd frontend
npm run lint
npm run build
```

Terraform 검증:

```cmd
cd infra\terraform
terraform fmt -check
terraform validate
```

현재 검증 기준은 백엔드 테스트 16개 통과, 프론트엔드 lint·production build 성공, Terraform validate 성공이다.
