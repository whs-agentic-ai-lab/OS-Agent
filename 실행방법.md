# 실행 방법

## 1. 일반 로컬 테스트

### 백엔드 실행

PowerShell에서 다음 명령을 실행한다.

```powershell
cd C:\Users\vinny\Desktop\whs_team\os-Agent-test\backend
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

정상 실행 확인:

```text
http://127.0.0.1:8000/api/health
```

`OPENROUTER_API_KEY`를 설정하지 않으면 로컬 테스트용 플래너가 사용된다.

### 프론트 실행

새 PowerShell 창에서 다음 명령을 실행한다.

```powershell
cd C:\Users\vinny\Desktop\whs_team\os-Agent-test\frontend
npm install
npm run dev -- --host 127.0.0.1
```

브라우저에서 다음 주소를 연다.

```text
http://127.0.0.1:5173
```

## 2. 대시보드에서 고정 AWS 환경 배포

다음 프로그램을 먼저 준비한다.

- Terraform `1.6` 이상
- AWS CLI v2
- 실행 중인 Docker Desktop
- `whs-team` 이름으로 구성된 AWS CLI profile

AWS 인증 확인:

```powershell
aws login --profile whs-team --region us-east-1
aws sts get-caller-identity --profile whs-team
```

`Your session has expired`가 나오면 `aws login`을 다시 실행한다.

그 다음 백엔드를 **로컬 주소로만** 다시 실행한다.

```powershell
cd C:\Users\vinny\Desktop\whs_team\os-Agent-test\backend
$env:DEPLOYMENT_ENABLED="true"
$env:AWS_PROFILE="whs-team"
$env:AWS_REGION="us-east-1"
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

프론트를 실행하고 `http://127.0.0.1:5173`의 `AWS 환경 배포` 버튼을 누른다. 확인 창을 승인하면 아래 작업이 자동으로 진행되고, 진행 로그와 Terraform output이 대시보드에 표시된다.

1. 고정 Terraform 초기화
2. ECR 생성
3. 백엔드 Docker 이미지 build/push
4. 고정 AWS 인프라 apply 및 EC2에서 백엔드 기동

`Terraform 초기화` 버튼은 고정 Terraform 디렉터리에서 `terraform init`만 실행한다. 공급자 플러그인과 작업 디렉터리를 미리 준비하거나 초기화 오류를 다시 확인할 때 사용한다.

`AWS 환경 삭제` 버튼은 환경 이름 `os-agent-test`를 정확히 입력하고 두 번째 확인을 통과해야 활성 작업을 시작한다. 동일한 로컬 Terraform state를 사용해 `terraform destroy`를 실행하며, ECR 이미지도 저장소와 함께 삭제한다. 삭제 작업 중에는 초기화나 배포를 동시에 실행할 수 없다.

대시보드는 Terraform 변수나 경로를 받지 않는다. 배포 대상은 `infra/terraform/fixed.auto.tfvars`의 `us-east-1`, `t3.small` 1대로 고정되어 있다. 이 작업은 NAT Gateway, EC2, VPC Endpoint와 CloudWatch Logs 등 **비용이 발생할 수 있는 AWS 리소스**를 만든다.

배포가 끝나면 워크플로우의 `05 SSM 포트 포워딩`을 선택하고 `SSM 자동 연결`을 누른다. Session Manager Plugin이 없으면 AWS 공식 Windows 설치 파일을 자동으로 내려받아 설치하며, 권한 확인 창이 표시될 수 있다. 설치 후 로컬 백엔드는 그대로 8000 포트에 유지되고 SSM 터널은 `127.0.0.1:8001 → EC2:8000`으로 연결된다. 에이전트 옵션·헬스 체크·실행 요청만 로컬 백엔드의 원격 게이트웨이를 거쳐 AWS 백엔드로 전달된다.

05번 노드는 AWS 백엔드 헬스 체크가 성공하면 자동으로 `완료`가 된다. `SSM 연결 종료`를 누르면 터널만 종료되고 로컬 백엔드는 계속 실행된다.

SSM 연결 과정에서 로컬 8000 포트는 FastAPI가 계속 사용하고, 터널은 별도 8001 포트를 사용한다. EC2 백엔드는 내부 Tool 대상망과 외부 API 통신망을 분리하며 호스트 8000 포트는 public inbound 없이 loopback에만 게시한다.

AWS access key나 secret key는 대시보드에 입력하지 않는다. 로컬 AWS CLI profile을 사용한다. 원격 EC2의 백엔드에서는 배포 기능이 기본적으로 꺼져 있다.

## 3. 종료

백엔드와 프론트를 실행한 각 PowerShell 창에서 `Ctrl+C`를 누른다.

대시보드의 `AWS 환경 삭제` 버튼을 사용하거나, 고정 Terraform 디렉터리에서 대상을 확인한 뒤 별도로 `terraform destroy`를 실행한다. 대시보드 삭제는 환경 이름 재입력과 추가 확인을 요구한다.

EC2만 정지하면 컴퓨팅 비용은 멈추지만 EBS, NAT Gateway, VPC Endpoint 등은 계속 과금될 수 있다. 며칠 이상 사용하지 않을 때는 `AWS 환경 삭제`로 전체 환경을 제거한다.

## 4. CMD에서 실행하는 경우

PowerShell의 `$env:NAME="value"` 문법은 CMD에서 동작하지 않는다. CMD에서는 아래처럼 실행한다.

```cmd
cd C:\Users\vinny\Desktop\whs_team\os-Agent-test\backend
set DEPLOYMENT_ENABLED=true
set AWS_PROFILE=whs-team
set AWS_REGION=us-east-1
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```
