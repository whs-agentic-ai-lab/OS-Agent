# 대시보드 추가사항

이번 작업에서 프론트엔드·대시보드 코드는 수정하지 않았다. 아래는 새 기능 아이디어가 아니라
현재 실행 상세 화면에서 Evidence를 연결할 때 필요한 최소 계약이다.

## 필요한 데이터와 필드

실행별 Evidence 목록에는 다음만 필요하다.

- 식별: `environment_id`, `event_id`, `context.run_id`, `context.action_id`
- 분류: `source_type`, `source`, `event_type`, `status`
- 시각·내용: `occurred_at`, `collector_received_at`, `message`
- 판정 구분: 기존 Supervisor executor 이벤트의 `payload.runtime_result/exit_code/stdout/stderr`와
  기존 백엔드 툴별 판정의 `payload.payload.verifier_result.{verifier,status,checks}`
- Snapshot/Artifact: `payload.phase/path_id/target_id/capture_event_id`,
  artifact의 `filename/status/size_bytes/sha256/original_sha256`

일반 OS 로그에는 실행 context가 null일 수 있다. 이 로그를 시간 근접성만으로 현재 run에
포함하면 안 된다. `status`는 수집 상태이고, 툴 결과나 Verifier 판정과는 별도다.

## 저장·제공 위치

- 이벤트: `public.evidence_events`, 유일키 `(environment_id,event_id)`
- 실행 연결: 기존 실행 테이블 및 `public.agent_runs`의 실제 run_id. 신규 통합 실행 테이블은 없다.
- Artifact 메타데이터: `public.evidence_artifacts`
- Artifact 본문: private Storage bucket `os-agent-evidence`
- 현재 FastAPI 구현: collector 전용 쓰기 API
  `/internal/evidence/events`, `/internal/evidence/artifacts/...`

브라우저에 Supabase 관리 키나 collector token을 제공하면 안 된다. Storage object_path는
공개 URL이 아니다.

## 기존 화면의 최소 변경

기존 실행 상세의 이벤트 영역에 실제 `environment_id + context.run_id`가 일치하는 Evidence를
시간순으로 합쳐 표시하고, tool result와 verifier result를 서로 다른 행으로 표시한다.
`parse_error`와 `collection_error`는 정상 `ok`와 구분한다. Artifact는 파일명·상태·크기·
저장본 SHA만 표시하면 된다.

## 현재 제공과 별도 구현의 구분

현재 제공:

- Vector 정규화 및 collector 전용 수신
- 이벤트/Artifact DB·Storage 저장 계약
- 기존 Supervisor 실행 context와 RunCoordinator/Harness의 기존 Verifier 반환값
- 업로더 호출 시의 상태·해시 계약. 자동 캡처→업로드 호출은 이번 이식에서 미연결

별도 구현 필요:

- 새 AgentOrchestrator ToolDefinition Verifier 상세 전달은 추가 연결 승인 전까지 미제공
- 브라우저 세션 권한으로 위 데이터를 읽는 Backend 조회 API
- 기존 실행 상세에서 그 조회 API를 호출하는 최소 연결
- private Artifact를 내려받아야 한다면 권한 확인 후 제한적으로 전달하는 Backend 경로

이번 범위에는 조회 API·다운로드 API·대시보드 코드를 추가하지 않았다.
