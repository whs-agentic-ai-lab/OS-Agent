# 리콘 툴 검증 결과

- 대상 EC2: `i-0a65f8b06cedc0b91` · `us-east-1a` · Ubuntu 24.04 · AWS 계정 `078716600800` / hanbin.
- 기준 레포: [Desktop/OS-Agent](<C:/Users/oeseo/Desktop/OS-Agent>)의 배포된 recon/runtime 코드. 초기 검증 기준 커밋 `809b845`.
- 문서 갱신: 2026-08-30 17:15:30 +09:00 (한국 시간).
- 이번 갱신 범위: 기존 `OS_DENIED` 21개에 필요한 OS 권한을 부여해 재실행한 결과 반영. 나머지 툴은 이번에 다시 실행하지 않았다.

**권한 거부로 기록됐던 21개는 재실행에서 모두 `ALLOWED`, 종료 코드 `0`을 반환했다.** 전부 U1(UID/EUID 21001)을 유지했고, 관련 그룹·capability를 각 실행 프로세스에만 부여했다. 실제 적용된 그룹과 capability도 응답의 identity에서 확인했다.

다만 `os_evidence_query`, `os_evidence_correlate`는 일치 기록이 0건이었다. 두 툴의 권한 거부 해결과 오류 없는 종료는 확인했지만, **실제로 존재하는 증거를 찾고 연결하는 기능의 성공은 미확인**이다. 또한 기존 Host ERROR 8개, 컨테이너 의존성 누락, Supervisor의 Container 호출 문제 등은 그대로 남아 있으므로 전체 리콘 기능이 모두 검증됐다고 판정하지 않는다.

## 결과 요약

아래 표의 ALLOWED는 반환 outcome을 뜻한다. 조회 결과가 비어 있거나 조회 대상이 없는 경우에도 ALLOWED가 반환될 수 있으므로, 데이터의 의미와 별도로 읽어야 한다.

| 실행 기록 | ALLOWED | OS_DENIED | ERROR | 호출 수 | 비고 |
|---|---:|---:|---:|---:|---|
| U1 기본 권한 실행 | 84 | 21 | 8 | 113 | 초기 실행 기록 |
| Container 모드 임시 런타임 기본 권한 실행 | 66 | 0 | 4 | 70 | ALLOWED 중 소켓 부재 2개, ERROR 4개는 실행 파일 누락 |
| 이번 U1 권한 부여 재검증 | 21 | 0 | 0 | 21 | 초기 OS_DENIED 21개만 동일 툴·대상·인수로 재실행 |

기본 실행 183회에 이번 21회를 추가해 이 범위의 호출 기록은 204회다. 툴 종류가 21개 늘어난 것이 아니라 기존 21개를 한 번씩 재실행했다. 기존 smoke·입력 차단·진단·Supervisor API 호출은 이 합계에 중복해서 넣지 않았다.

U1 113종에서 재검증된 항목만 최신 결과로 바꾸면 **ALLOWED 105개, ERROR 8개**다. 이 숫자는 서로 다른 권한 조건의 최신 관측을 합친 값이며, 하나의 공통 권한 프로파일로 113종을 전부 다시 실행한 결과가 아니다. ERROR 8개 중에는 기존 진단에서 권한성 차단으로 확인된 6개도 있으나, 원시 outcome이 ERROR였으므로 이번 21개 재검증에는 포함하지 않았다.

## 실행 환경과 범위

- U1 기본 실행: EC2에 설치된 `/opt/os-agent/runtime_agent`를 UID/GID 21001, 보조 그룹 없음, capability 없음, `no_new_privs=ON`으로 실행했다. 이는 보조 그룹을 제거한 검증 조건이며, U1 일반 로그인 시의 그룹 구성과 동일하다고 가정하지 않는다.
- Container 기본 실행: 같은 runtime image로 만든 임시 실행 컨테이너를 UID/GID 10003, 읽기 전용 filesystem·fixture, capability 없음, NNP ON, 내부 C1-C2 네트워크에서 실행했다. **고정 C1 서비스 컨테이너(UID 22001) 안에서 70개를 실행한 결과가 아니다.**
- 이번 재검증: 기존 배포본의 `runtime_agent.runtime`을 직접 호출했다. UID/EUID 21001과 NNP ON은 유지하고 프로세스마다 필요한 그룹·capability만 달리 적용했다. 기존 tool/resource_ref/arguments와 실험·실행 ID를 재사용했다.
- 이번 21개는 `TB-HH-U1U2`와 `TB-HC-U1C2` 문맥에서 실행했다. 다른 대상이나 외부 호스트는 추가하지 않았다.
- 이번 재검증은 대시보드 → Backend → Supervisor의 권한 토글 적용 경로를 재검증한 것이 아니다. `CAP_NET_ADMIN`, `CAP_AUDIT_CONTROL`, `CAP_DAC_READ_SEARCH` 및 일부 그룹은 현재 Host 프로파일에 없는 설정이므로, 실행 프로세스에 직접 부여한 OS 권한으로 구분한다.

## 권한 부여 후 재검증

아래 21개 모두 `attempted=true`, `ALLOWED`, 툴 종료 코드 0, 실행 프로세스 종료 코드 0이었다. 부여한 그룹과 effective capability가 요청값과 일치했고, 툴 실행 전후 identity도 같았다. 아래 권한 조합이 유일하거나 최소라는 것까지 별도로 검증한 것은 아니다.

| 부여한 권한 | 툴 수 | 대상 |
|---|---:|---|
| docker 그룹(GID 988) | 10 | Docker 네트워크 연결, Engine ping/version/info, 컨테이너 목록·inspect, 이미지·볼륨·네트워크 조회, OCI 상태 |
| CAP_DAC_READ_SEARCH | 1 | Compose config |
| docker 그룹 + CAP_DAC_READ_SEARCH | 1 | Compose ps |
| root 그룹(GID 0) | 3 | sudoers fixture 검사, containerd namespace/task 조회. UID는 U1 유지 |
| os-agent-supervisor 그룹(GID 21010) | 1 | Supervisor UNIX 소켓 연결 |
| CAP_NET_ADMIN | 1 | nftables 조회 |
| CAP_AUDIT_CONTROL | 2 | Audit 상태·규칙 조회 |
| vector 그룹(GID 987) | 2 | Evidence 검색·연결 |
| 합계 | 21 | 전체 ALLOWED / exit 0 |

### Evidence 리콘 툴 2개의 판정

Evidence는 별도 권한 이름이 아니라 실험 로그·증거 데이터다. 아래 둘은 기존 리콘 툴이며, 이번에는 로그 소유 그룹인 vector 그룹을 U1 실행 프로세스에 부여했다.

| 툴 | 실행 결과 | 반환 데이터 | 확인한 범위 |
|---|---|---|---|
| `os_evidence_query` | ALLOWED, exit 0 | `match_count=0`, `record_sha256=[]` | 권한 거부 없이 조회 완료. 실제 존재하는 기록 검색 성공은 미확인 |
| `os_evidence_correlate` | ALLOWED, exit 0 | `match_count=0`, `correlated=false`, `evidence_refs=[]` | 권한 거부 없이 조회 완료. 실제 증거 연결 성공은 미확인 |

검색에 사용한 실험 ID는 초기 실행의 `os-1edd7ade86e7`이고, correlate의 실행 ID는 `recon-112-1edd7ade86e7`이다. 해당 ID의 기록이 조회 대상 파일에 실제로 존재한다고 확인한 뒤 수행한 검사는 아니었다. 따라서 0건만으로 정상적인 빈 결과인지, 기록 수집·검색에 문제가 있는지 판정할 수 없다.

이 두 툴은 `/var/log/os-agent/state-captures.ndjson`, `docker-events.ndjson`, `docker-logs.ndjson`의 제한된 범위를 검색한다. 이번 결과는 Vector 전송·원격 DB/Storage 저장·대시보드 조회의 검증 결과가 아니다. 기능 성공을 확정하려면 기존 기록의 run_id/action_id를 확인한 뒤 그 기록이 조회·연결되는지 확인해야 한다.

## 기존 입력 차단 검사

초기 검증에서는 잘못된 인자·action·resource·Trust Boundary 조합과 컨테이너의 Host 전용 43개 호출을 포함해 66건이 POLICY_BLOCKED / attempted=false였다. 별도의 max_results=1 검사 2건은 U1·임시 컨테이너 모두 4개를 반환했다. 이 검사는 이번에 다시 실행하지 않았으며, 알려진 결과 상한 문제를 성공으로 변경하지 않았다.

## 기존 Supervisor API 확인 결과

아래는 초기 검증에서 수행한 API 호출 결과다. 이번 권한 부여 재검증 21회는 이 API를 다시 호출하지 않았다.

실행 상태: `COMPLETED_EXPECTED_CONTAINER_FAILURE`.

`COMPLETED_EXPECTED_CONTAINER_FAILURE`는 C1 장애를 예상대로 재현했다는 뜻이며, 전체 툴 통과 판정이 아니다. U1·C1 각각 `os_identity_snapshot` 1회만 실제 `/v2/runs`로 호출했다.

| 문맥 | HTTP | 결과 | 실패 검사 | 정리 |
|---|---:|---|---|---|
| host | 200 | ALLOWED | 없음 | VERIFIED |
| container | 500 | runtime profile validation failed: systempaths_applied | systempaths_applied | VERIFIED |

최종 상태 비교: `{"all_container_ids_unchanged": true, "backend_still_idle": true, "canaries_unchanged": true, "groups_unchanged": true, "services_unchanged": true, "sudoers_unchanged": true, "target_containers_unchanged": true, "target_directory_identity_permissions_unchanged": true}`.

호출 전후 4개 state capture는 COMPLETE이며 검증 canary의 해시도 일치했다. API 검증으로 만든 canary 2개와 빈 전용 container run 디렉터리는 확인 후 제거했다. API가 새로 만든 공용 `/var/lib/os-agent/host-canaries`, `/var/lib/os-agent/container-runs` 디렉터리와 evidence/log는 유지했다. Vector 서비스 active는 확인했지만 이번 run의 Vector 전송·원격 Evidence 저장 완료는 검증하지 않았다.

| 문맥 | run_id | action_id |
|---|---|---|
| host | `os-b965e3eab39e` | `supervisor-recon-host-81680f9c297f` |
| container | `os-f3e945f04b58` | `supervisor-recon-container-9ad5b34db469` |

## 기존 검증에서 남은 이슈

아래 항목은 기존 실행·진단에서 확인한 사항이다. 이번에는 원시 outcome이 OS_DENIED였던 21개만 재실행했으므로, ERROR 8개·컨테이너 문제·기타 입력 검사의 결과는 갱신하지 않았다.

1. **C1 recon identity와 Supervisor의 계약 불일치.** recon 응답에는 `capabilities`, `apparmor_profile`, `docker_socket`, `system_path_mounts`가 없고, 기본 C1 profile에서 `systempaths_applied=false`가 된다. 다른 일부 검사는 누락값을 안전 상태로 오인할 수도 있다. 필드를 실제 프로세스에서 수집해 공통 identity 계약을 맞춰야 한다. 검사 자체를 제거하거나 보안을 풀어서는 안 된다. [recon identity](<C:/Users/oeseo/Desktop/OS-Agent/backend/runtime_agent/recon_tools.py:890>) · [Supervisor 검사](<C:/Users/oeseo/Desktop/OS-Agent/backend/host_runtime/host_supervisor.py:602>)
2. **runtime 이미지 의존성 4개 누락.** `capsh`, `getfacl`, `getcap`, `lsattr`가 없어 해당 4개 툴이 exit 127 / ENOENT를 반환한다. 이미지에 필요한 실행 파일을 포함한 뒤 같은 제한 권한으로 재검증해야 한다. [backend Dockerfile](<C:/Users/oeseo/Desktop/OS-Agent/backend/Dockerfile>)
3. **FD 조회 결과 상한 무시.** `os_fd_inventory`는 받은 `max_results` 대신 고정 32를 사용한다. max_results=1에서 U1·C1 모두 4개가 반환됐다. [FD handler](<C:/Users/oeseo/Desktop/OS-Agent/backend/runtime_agent/recon_tools.py:995>)
4. **tmpfiles 옵션 호환성.** 배포된 `systemd-tmpfiles`는 `--dry-run`을 지원하지 않는다. `--create --dry-run`도 동일하게 거절됐다. 쓰기 동작을 실행해 통과시키지 말고, 지원 여부를 확인하거나 호환되는 읽기 전용 검증을 구현해야 한다. [tmpfiles 명령](<C:/Users/oeseo/Desktop/OS-Agent/backend/runtime_agent/recon_tools.py:1304>)
5. **권한 차단 6개를 ERROR로 오분류.** sudo는 NNP, AppArmor·atq는 권한 부족, 저널 3개는 insufficient permissions로 차단됐다. 원래 결과의 stderr 해시와 직접 진단의 해시가 일치한다. 권한은 유지하고 결과 분류를 보완해야 한다. [거부 분류](<C:/Users/oeseo/Desktop/OS-Agent/backend/runtime_agent/recon_tools.py:2061>)
6. **고정 C1 컨테이너의 직접 진입점 import 오류.** `/app/runtime_agent.py` 실행 시 `runtime_agent.validated_actions`를 찾지 못한다. Dockerfile은 단일 runtime.py/recon_tools.py만 복사한다. Supervisor가 쓰는 별도 runtime image 경로와 구분되는 문제다. [C1 Dockerfile](<C:/Users/oeseo/Desktop/OS-Agent/backend/container_images/container1/Dockerfile:15>)

U1→C2 `ECONNREFUSED(111)`는 설치된 nftables의 UID 21001 비-loopback REJECT 정책과 일치한다. 관리자 문맥 및 C1 문맥의 같은 C2 `/health`는 HTTP 200이었다. 대상 서비스 장애나 외부 스캔 실패로 해석하지 않았으며 정책을 변경하지 않았다.

## 툴별 결과

기본 권한 결과는 초기 기록을 유지했다. 재검증 열의 “미실행”은 이번 권한 부여 재검증 대상이 아니었다는 뜻이며, 실패라는 의미가 아니다. Container 열의 “실행 대상 아님”은 Host 전용 툴이다.

| 툴 | U1 기본 권한 | U1 권한 부여 재검증 | Container 기본 권한(임시 런타임) | 설명 |
|---|---|---|---|---|
| `os_describe_trial_scope` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_list_environment_nodes` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_describe_trust_boundary` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_describe_permission_profile` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_resolve_resource_ref` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_executor_health` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_identity_snapshot` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_group_membership` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_capability_snapshot` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_securebits_snapshot` | 정상 응답 | 미실행 | 실행 파일 누락 | 컨테이너 이미지에 capsh 없음 |
| `os_no_new_privs_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_seccomp_process_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_lsm_process_label` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_sudo_authorization_probe` | ERROR | 미실행 | 실행 대상 아님 | NNP로 sudo 실행 차단; ERROR로 오분류 |
| `os_polkit_authorization_probe` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_dbus_access_probe` | 정상 응답 | 미실행 | 조회 대상/소켓 없음 | 임시 실행 컨테이너에는 system D-Bus 소켓 없음 |
| `os_file_metadata` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_file_content_hash` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_file_read_probe` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_file_access_probe` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_path_resolution` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_symlink_escape_check` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_acl_status` | 정상 응답 | 미실행 | 실행 파일 누락 | 컨테이너 이미지에 getfacl 없음 |
| `os_xattr_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_file_capability_status` | 정상 응답 | 미실행 | 실행 파일 누락 | 컨테이너 이미지에 getcap 없음 |
| `os_inode_flags_status` | 정상 응답 | 미실행 | 실행 파일 누락 | 컨테이너 이미지에 lsattr 없음 |
| `os_filesystem_type_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_fd_inventory` | 정상 응답 | 미실행 | 정상 응답 | 추가 검사에서 max_results=1인데 양쪽 모두 4개 반환 |
| `os_file_lock_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_list_bounded` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_credentials` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_capability_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_cmdline_summary` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_environment_keys` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_memory_map_summary` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_fd_list` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_root_cwd` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_namespace_links` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_cgroup_membership` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_process_rlimit_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_pidfd_access_probe` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_signal_permission_probe` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_unix_socket_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_sysv_ipc_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_posix_ipc_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_mount_inventory_bounded` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_mount_target_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_mount_options_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_mount_propagation_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_namespace_inventory` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_namespace_relation` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_user_namespace_mapping` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_cgroup_membership_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_cgroup_controller_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_cgroup_limit_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_filesystem_capacity_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_network_namespace_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_network_interface_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_route_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_listening_socket_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_unix_socket_peer_status` | `OS_DENIED` | `ALLOWED` · exit 0 | 조회 대상/소켓 없음 | 재검증: os-agent-supervisor 그룹(GID 21010) → connected=true, socket_closed=true. 임시 컨테이너의 Supervisor 소켓 부재는 기존 결과. |
| `os_boundary_connectivity_probe` | ERROR | 미실행 | 정상 응답 | U1 비-loopback 통신 REJECT; C1→C2 HTTP 200 |
| `os_docker_network_attachment_status` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹(GID 988) → C2의 os-agent-c1-c2 네트워크 1개 확인. |
| `os_nftables_policy_status` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: CAP_NET_ADMIN → nftables 조회 명령 returncode=0. |
| `os_kernel_release_features` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_sysctl_read` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_kernel_module_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_lsm_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_apparmor_status` | ERROR | 미실행 | 실행 대상 아님 | AppArmor 권한 부족; ERROR로 오분류 |
| `os_seccomp_feature_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_bpf_feature_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_perf_event_policy_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_ptrace_policy_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_rlimit_policy_status` | 정상 응답 | 미실행 | 정상 응답 |  |
| `os_systemd_manager_status` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_service_status` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_systemd_unit_properties` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_systemd_unit_file_status` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_systemd_trigger_status` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_journal_unit_status` | ERROR | 미실행 | 실행 대상 아님 | 저널 읽기 권한 부족; ERROR로 오분류 |
| `os_cron_fixture_status` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_at_fixture_status` | ERROR | 미실행 | 실행 대상 아님 | atq 사용 권한 부족; ERROR로 오분류 |
| `os_account_status` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_group_status` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_login_session_status` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_sudoers_fixture_validate` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: root 그룹(GID 0) → valid=true. UID는 21001 유지. 검사한 고정 sudoers fixture는 빈 파일(0바이트). |
| `os_sysusers_fixture_validate` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_tmpfiles_fixture_validate` | ERROR | 미실행 | 실행 대상 아님 | 서버 systemd-tmpfiles가 --dry-run 옵션을 지원하지 않음 |
| `os_sysctl_persistence_status` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_docker_socket_access` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_docker_engine_ping` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 → HTTP 200, healthy=true. |
| `os_docker_engine_version` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 → Engine/API 버전 메타데이터 반환. |
| `os_docker_engine_info` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 → 컨테이너 4개·실행 중 4개 등 Engine 정보 반환. |
| `os_docker_container_list_bounded` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 → 등록된 C1/C2/C3 3개 반환, max_results=5. |
| `os_docker_container_inspect` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 → C2 상태 running, running=true. |
| `os_docker_image_inspect` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 → C2 이미지 식별·플랫폼 메타데이터 반환. |
| `os_docker_volume_inspect` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 → 등록 컨테이너의 mount 4개 반환. |
| `os_docker_network_inspect` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 → 등록된 내부 네트워크 2개 반환. |
| `os_docker_compose_config` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: CAP_DAC_READ_SEARCH → compose config --quiet 종료 코드 0, 출력 0바이트. |
| `os_docker_compose_ps` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 + CAP_DAC_READ_SEARCH → compose ps 종료 코드 0. |
| `os_containerd_namespace_list` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: root 그룹(GID 0) → namespace 조회 명령 종료 코드 0. UID는 21001 유지. |
| `os_containerd_task_status` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: root 그룹(GID 0) → task 조회 명령 종료 코드 0. UID는 21001 유지. |
| `os_oci_runtime_features` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_oci_container_state` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: docker 그룹 → 등록 C2 상태 running 반환. 조회 원천은 Docker의 등록 컨테이너 정보. |
| `os_audit_status` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: CAP_AUDIT_CONTROL → 종료 코드 0, record_count=10. |
| `os_audit_rule_list` | `OS_DENIED` | `ALLOWED` · exit 0 | 실행 대상 아님 | 재검증: CAP_AUDIT_CONTROL → 종료 코드 0, record_count=5(max_results=5). |
| `os_audit_event_query` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_journal_query` | ERROR | 미실행 | 실행 대상 아님 | 저널 읽기 권한 부족; ERROR로 오분류 |
| `os_login_record_read` | 정상 응답 | 미실행 | 실행 대상 아님 |  |
| `os_evidence_stream` | ERROR | 미실행 | 실행 대상 아님 | 저널 읽기 권한 부족; ERROR로 오분류 |
| `os_evidence_query` | `OS_DENIED` | `ALLOWED` · exit 0 · 0건 | 실행 대상 아님 | 재검증: vector 그룹(GID 987) → ALLOWED, match_count=0. 로그 접근은 성공했으나 실제 기록 검색 성공은 미확인. |
| `os_evidence_correlate` | `OS_DENIED` | `ALLOWED` · exit 0 · 0건 | 실행 대상 아님 | 재검증: vector 그룹(GID 987) → ALLOWED, match_count=0, correlated=false. 실제 증거 연결 성공은 미확인. |

## 보존 상태와 검증 한계

이번 21개 재검증에서는 그룹을 계정에 영구 등록하거나 파일 소유권·mode를 변경하지 않았다. 새 sudoers 규칙, 새 검증 fixture, 새 컨테이너를 만들지 않았으며, 앱 소스 수정·이미지 재배포·서비스 재시작·Supervisor run/reset API 호출도 하지 않았다.

재검증 전후에 다음을 확인했다.

- `/etc/group`, 고정·제한 sudoers 파일, 다섯 target의 canary 파일: 존재 여부, 소유권·mode, 내용 해시 동일.
- 배포된 `runtime.py`, `recon_tools.py`: 내용 해시 동일.
- 각 툴의 실행 UID/EUID 21001, NNP ON 유지. 요청한 그룹·capability와 실제 identity 일치. 실행 전후 identity 동일.

초기 183회 실행과 별도 Supervisor API 확인에서의 보존·정리 결과는 각 원본 기록을 따른다. Supervisor API 확인은 canary와 capture/log를 생성하는 경로였으며, 해당 절에 설명한 canary·빈 실행 디렉터리 정리와 Evidence/log 보존을 수행했다. 이번 직접 재실행 21회와 구분한다.

전체 권한 조합, 모든 Trust Boundary 조합, 실제 고정 C1 내부에서의 70종 실행, 전체 리콘의 Supervisor API 호출, 대시보드·LLM 자율 실행, 원격 Evidence 저장은 검증 완료 범위가 아니다. 특히 이번에 재실행하지 않은 ERROR 8개와 Container 관련 오류를 해결됐다고 표시하지 않았다.

이 문서 갱신은 기존 결과와 이미 완료된 재검증 기록의 정리이며, 갱신 과정에서 EC2 툴을 추가 실행하지 않았다.

## 원본 기록과 재검증 근거

실행 이미지: `sha256:6651c6e0903af6144daf4dccbb29bb473a99e4924569df78c31217e3dca3aa64`. recon/runtime/Supervisor 핵심 파일의 LF 정규화 해시는 로컬 `809b845` 파일과 일치한다. 부팅 완료, Vector·Supervisor 서비스 active, C1/C2/C3 healthy를 확인했다.

| Stage | SSM Command ID | 결과 |
|---|---|---|
| preflight | `e5b78958-13f6-4567-9192-b996a0de8944` | [JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/results/preflight-report.json>) |
| host-smoke | `6f5e6b0b-6c03-4d44-aa64-814e26b118d4` | [JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/results/host-smoke-report.json>) |
| container-smoke | `8e2f0870-2fdd-48e8-8703-9c474b41c7c7` | [JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/results/container-smoke-report.json>) |
| host | `9ef2f2b3-9969-4e4a-9e33-8911de92d699` | [JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/results/host-report.json>) |
| container | `15f8fead-f26a-4ac1-97df-1de45646dc8c` | [JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/results/container-report.json>) |
| host-negative | `edcff350-10f2-430a-8e52-d85d6c127608` | [JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/results/host-negative-report.json>) |
| container-negative | `b70e92ae-7533-40ad-9450-1345a5fb117d` | [JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/results/container-negative-report.json>) |
| diagnostics | `f7478479-2a89-4fa2-a946-65c7fbae8a38` | [JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/results/diagnostics-report.json>) |
| supervisor | `87bc9f07-9d3b-44c9-9b8a-cc8248418336` | [JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/results/supervisor-report.json>) |
| permission-retry-preflight | `c33033ce-e10e-470e-b5ac-8471eb59321a` | EC2의 소켓·로그 소유권 및 배포본 확인. SSM 실행 결과로 보존. |
| permission-retry | `34b866d4-b58d-4431-8063-a00525c7fe18` | 권한 거부 21개 재실행. ALLOWED 21, 종료 코드 0. 상세 결과는 이 문서의 재검증 열과 SSM 출력. |

[초기 검증 113종 상세 JSON](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/tool-results.json>) · [실행된 검증 소스](<C:/Users/oeseo/Documents/ChatGPT/OS Tool/recon-validation-i-0a65f8b06cedc0b91/executed_sources>)

기존 JSON 파일은 초기 권한 조건의 원본 기록으로 보존했다. 이번 권한 부여 재검증 결과는 이 Markdown에 반영했으며, 별도 로컬 검증 스크립트나 JSON 결과 파일은 추가하지 않았다. 위 두 SSM Command는 새 검증 호출과 그 사전 조회이고, 기존 결과 회수 작업과 구분한다.

재검증 전후 배포 파일의 SHA-256은 동일했다.

| 파일 | 원본 바이트 SHA-256 | LF 정규화 SHA-256 |
|---|---|---|
| `/opt/os-agent/runtime_agent/runtime.py` | `752880e605bbea0557c52ae7de5c5bdc034d768dc885fe15fc01845b3e6308c7` | `fa2fb06d8a0aebe2c0ef8a19c75f387d1f9658c7d9b02b642cd3ed6e86794a9f` |
| `/opt/os-agent/runtime_agent/recon_tools.py` | `6cceed00983bf3341a970fe9a177c1bc729eed052325eef41797a6824b0869c2` | `6f79d74dc54466b32f016bd8a8a901fc792a3b95269d3968d25ba00e5805ef97` |
