# 다른 팀 Agent 기능의 OS Agent 적용 계획

- 작성일: 2026-08-24
- 대상 저장소: os-Agent-test
- 문서 상태: Harness Core 우선 적용 — OS 권한·Tool·Verifier Adapter는 후속 연결
- 비교 대상: whs-agentic-ai-lab/Ai-Agent 원격 기능 브랜치와 현재 os-Agent-test 작업 트리

## 1. 목적

현재 OS Agent는 Container와 Ubuntu Host의 실제 권한 경계를 안전하게 적용하고, 환경 내부 Runtime Agent가 고정 Tool을 한 번 실행한 뒤 Control Backend가 결과를 판정하는 최소 실험 구조를 갖고 있다.

다른 팀의 공통 Agent Harness에는 다음과 같은 자율 Agent 기능이 추가되어 있다.

~~~text
Recon
→ State 정규화
→ Capability 추론
→ Action Frontier 생성
→ Planner 선택
→ Tool 실행
→ Independent Verification
→ Reset
→ State 갱신과 Re-plan
~~~

이 문서의 목적은 해당 기능이나 코드를 그대로 복사하는 것이 아니다. 다른 팀에서는 Agent Harness의 공통 개념과 계약만 참고하고, OS 권한 실험에 직접 필요한 기능만 선별하여 현재의 실제 실행 경계와 안전장치를 보존한 채 외부 계층으로 결합한다.

이 계획에서 "결합"은 기존 OS Agent를 교체하거나 내부 동작을 재작성한다는 의미가 아니다. 기존 실행 경로는 그대로 유지하고, Control Backend가 그 위에서 정찰 State·Capability·Frontier·재계획·Evidence를 조정하는 Harness 역할을 추가한다는 의미다.

최종 목표는 다음과 같다.

> Agent가 승인된 OS 환경과 Resource 안에서 현재 Identity·파일·그룹·sudo·mount·capability·service 상태를 정찰하고, 가능한 권한 경로를 Capability로 조합하여 다음 안전한 Action을 선택하며, 실제 효과와 독립 Evidence를 기준으로 재계획할 수 있게 한다.

### 1.1 현재 결정

Harness Core는 지금 적용한다. 다만 아직 확정되지 않은 OS 권한·Tool·Verifier의 구체적인 내용을 Harness 안에 임시로 만들거나 고정하지 않는다.

지금 구현하는 범위는 다음과 같다.

- Harness Run과 상태 수명주기
- 구성요소 Port/Adapter 계약
- Component readiness와 누락 구성요소 표시
- 반복·Tool 호출·시간 Budget
- 구조화 Event와 종료 사유
- 기존 OS 단일 Run과 분리된 Harness API
- In-memory 저장과 테스트용 Fake Adapter 주입점

다음 항목은 나중에 OS 팀 구현이 확정되면 Adapter로 연결한다.

1. Permission Provider: 권한 Profile과 실제 적용 상태 제공
2. Tool Catalog/Executor: 최종 Tool 목록과 실제 실행
3. Independent Verifier/Resetter: 독립 Evidence 판정과 복구

구성요소가 연결되지 않은 Harness Run은 성공을 가장하지 않고 BLOCKED와 MISSING_REQUIRED_COMPONENTS를 반환한다.

## 2. 현재 OS Agent 기준선

현재 구현은 최소 환경 시험용 초안을 포함한다. 그중 실행·보안·운영 구조는 유지 대상이고, 권한·Tool·Verifier의 구체적인 내용은 아직 확정 전이다.

현재 확인된 구조는 다음과 같다.

- 실행 경계: container, host
- 환경별 완전한 권한 Profile 묶음
  - Container: mount_write, run_as_root, dac_override
  - Host: owner_write, group_write, limited_sudo
- Runtime Agent는 선택한 실제 경계 안에서 비특권 프로세스로 실행
- Host의 root 작업은 root-owned Supervisor만 수행
- Control Backend와 Agent Runtime은 Docker socket 또는 임의 root 권한을 받지 않음
- 승인된 profile-canary, nginx-target, 제한된 sudo helper만 사용
- 현재 Tool: file_read, file_write, service_status
- Control Backend가 Run 수명주기, 결과 수집, 판정과 Supabase 저장을 담당
- AWS 환경은 private subnet과 SSM Port Forwarding으로만 접근
- Terraform 배포·삭제, ECR image build/push, EC2/SSM 운영 기능 유지
- 실제 Key와 Credential은 LLM Context, Frontend, Run Event에 기록하지 않음

아래 항목은 현재 코드가 있더라도 최종 확정된 것으로 간주하지 않는다.

- Container/Host 권한 Profile의 최종 항목과 조합 방식
- Agent가 사용할 최종 Tool 목록과 Tool별 입력·출력 계약
- PASS/FAIL/INCONCLUSIVE 또는 VERIFIED/REJECTED 판정 기준
- 독립 Evidence 수집 위치와 신뢰 경계
- Action별 Reset 범위와 완료 조건

### 2.1 변경 금지 장점

다음 항목은 개선 대상이 아니라 비회귀 계약이다. Harness 기능을 추가하더라도 동작·권한·운영 방식이 달라지면 안 된다.

| OS Agent의 현재 장점 | 변경 금지 조건 |
| --- | --- |
| 실제 Container/Host 권한 적용 | 모의 판정이나 Backend 로컬 실행으로 대체하지 않는다. |
| 환경 내부 Runtime Agent | Planner/Executor/Tool의 실제 실행 위치를 Control Backend로 옮기지 않는다. |
| root-owned Host Supervisor | root 권한 적용 책임과 allowlist 검증 책임을 다른 프로세스로 분산하지 않는다. |
| 비특권 Backend | Docker socket, privileged container, 광범위 sudo 권한을 추가하지 않는다. |
| Supervisor만 권한 적용 | 최종 Profile이 바뀌더라도 LLM이나 Runtime Agent가 권한을 직접 변경하게 하지 않는다. |
| Resource allowlist 원칙 | 최종 Tool이 바뀌더라도 임의 파일 경로, 프로세스, service 이름을 허용하지 않는다. |
| 독립된 Container/Host Trial | 한 Run이 경계 사이를 자동 이동하거나 다른 Host로 확산하지 않는다. |
| Private subnet과 SSM 전용 접근 | Agent 기능 때문에 public inbound나 별도 공개 API를 추가하지 않는다. |
| Terraform 배포·삭제 자동화 | 기존 ECR build/push, EC2 선택, 전체 삭제 흐름을 교체하지 않는다. |
| Windows setup/run 자동화 | 팀원의 현재 설치·실행 진입점을 유지한다. |
| Supabase 로그와 메모리 fallback | 저장소를 PostgreSQL 등 다른 제품으로 교체하지 않는다. |
| 7단계 운영 워크플로우 | 기존 배포·연결·실행·종료 상태와 수동 복원 기능을 유지한다. |
| 비밀정보 분리 | OpenRouter Key, Supabase Secret, AWS Credential을 Runtime/LLM/Frontend로 확대 전달하지 않는다. |

신규 Harness 기능 때문에 위 계약 중 하나라도 깨지면 해당 변경은 기능이 동작하더라도 완료로 인정하지 않는다.

현재 구조의 핵심 한계는 다음과 같다.

- Prompt 하나당 Tool 하나를 선택하고 실행이 종료된다.
- 환경 정찰 결과를 다음 실행에서 사용할 구조화 State가 없다.
- OS 권한을 Capability와 경로로 조합하지 않는다.
- Action 후보 목록인 Frontier와 실패 후 Re-plan이 없다.
- Verifier가 Runtime이 반환한 hash와 결과를 판정하며, 별도 실행 경계에서 상태를 다시 수집하지 않는다.
- Action별 Reset 성공을 다음 Action 진행 조건으로 강제하지 않는다.
- 반복 횟수, Tool 호출, 재시도와 무진전 종료 예산이 없다.

## 3. 적용 원칙

### 3.1 유지할 경계

1. LLM은 Tool 이름과 구조화된 인자만 선택한다.
2. LLM에 shell, Python 코드 실행, 임의 파일 경로, Credential을 제공하지 않는다.
3. 모든 Resource는 논리적 Resource ID에서 Supervisor 소유 실제 대상 경로로 변환한다.
4. 권한 Profile 적용은 Agent가 아니라 Supervisor가 담당한다.
5. Agent의 성공 주장이나 Tool의 exit code만으로 성공을 확정하지 않는다.
6. 상태 변경 Action은 Independent Verification과 Reset이 모두 완료돼야 다음 Action으로 진행한다.
7. Container와 Host를 한 Campaign에서 자동으로 오가지 않는다. 각 경계를 별도 Trial로 실행하고 결과만 비교한다.

### 3.2 도입 순서

다음 순서를 강제한다.

~~~text
공통 계약 정리
→ 읽기 전용 Recon
→ Capability와 Frontier
→ 독립 Evidence와 Reset
→ 반복 Campaign과 Budget
→ UI·저장 확장
→ 선택적 Multi-Agent
~~~

Multi-Agent를 먼저 붙이지 않는다. 단일 Planner의 State → Frontier → Action → Verify → Reset → Re-plan 흐름을 먼저 완성해야 역할을 나눠도 동일한 안전 계약을 사용할 수 있다.

### 3.3 Harness 결합 경계

다른 팀에서 참고하는 범위는 다음 공통 Harness 계층으로 제한한다.

~~~text
참고 대상
  State 계약
  Recon 결과 정규화
  Capability 평가
  Action Frontier
  Planner 출력 검증
  Campaign Budget과 종료
  Independent Evidence 계약
  Action별 Reset orchestration

참고하지 않는 대상
  AWS/ECR/ECS/EKS 개별 Tool
  IAM Role/Policy 해석 로직
  Kubernetes Trust Boundary 구현
  CloudTrail/PMapper/CloudFox 연동
  PostgreSQL 저장 구조
  다른 팀의 배포 토폴로지
  생성 코드 자동 실행
~~~

Harness는 다음 위치에 추가한다.

```text
새 Harness 계층
    ↓ 기존 계약으로 요청
기존 RunCoordinator / RuntimeClient
    ↓ 변경 없는 실행 경로
기존 root Supervisor / Environment Runtime
```

초기 구현에서는 기존 POST /api/runs와 Runtime dispatch payload를 유지한다. 신규 Campaign API가 기존 단일 Run을 조합하여 호출하도록 만들고, 실제 실행 경계를 다시 구현하지 않는다.

### 3.4 기능 선별 기준

다른 팀 기능은 아래 질문을 모두 통과할 때만 적용한다.

1. OS 권한 경계를 더 잘 발견하거나 설명하는가?
2. owner/group/sudo/mount/UID/capability 조건을 경로로 연결하는 데 필요한가?
3. Agent 판단과 실제 OS Effect를 더 정확히 분리하는가?
4. 반복 실험의 안전성·재현성·비교 가능성을 높이는가?
5. 현재 root Supervisor, 비특권 Runtime, SSM, Terraform, Supabase 장점을 그대로 유지하는가?

1~4 중 명확한 효과가 없으면 추가하지 않는다. 5를 만족하지 못하면 효과가 있어도 제외한다.

## 4. 다른 팀 기능별 적용 판단

| 다른 팀 기능 | OS Agent 적용 방식 | 우선순위 | 판단 |
| --- | --- | --- | --- |
| 정규화된 Agent State | OS Identity, Asset, Authorization, Capability, Evidence State로 변환 | P0 | 적용 |
| Permission-aware Recon | OS Profile과 실제 상태를 이용한 Recon Scheduler로 변환 | P0 | 적용 |
| Capability 추론 | 파일·그룹·sudo·mount·capability 조건 조합 규칙으로 변환 | P0 | 적용 |
| Action Frontier | 현재 State에서 실행 가능한 안전한 Tool 후보 생성 | P0 | 적용 |
| 실패 후 Re-plan | DENIED, NOT_FOUND, INVALID_INPUT, TIMEOUT별 재계획 | P0 | 적용 |
| Independent Verifier | 기존 Control Verifier를 유지하고 Supervisor의 별도 read-only Snapshot Evidence만 추가 | P0 | 강화 적용 |
| Action별 Reset | 기존 Canary/Profile 복구 방식을 유지하면서 완료 Evidence와 진행 차단 조건만 Harness에 추가 | P0 | 적용 |
| Engagement/Scenario Guardrail | Host ID, 경계, Tool, Resource, 위험도, 예산을 고정 | P0 | 적용 |
| Tool Registry Schema 검증 | Tool 입력 Schema, 위험도, Resource field, verifier/resetter 등록 | P0 | 적용 |
| Campaign Budget | 반복, Tool 호출, 재시도, 시간, 무진전 상한 | P1 | 적용 |
| Attack/Capability Graph UI | OS 권한 경로와 Evidence를 그래프로 표현 | P1 | 적용 |
| 역할별 Context 분리 | Planner에는 최소 State, Verifier에는 검증 입력만 전달 | P1 | 적용 |
| Multi-Agent Explorer/Critic | 단일 Agent 루프 검증 후에도 OS 실험 정확도 개선이 입증될 때만 도입 | P2 | 조건부 |
| Tool Discovery Proposal | 새 Tool 명세 초안만 생성하고 자동 등록·실행 금지 | P2 | 제한 적용 |
| AWS/ECR/ECS/EKS Tool | OS 실행환경과 무관 | 제외 | 미적용 |
| CloudTrail/PMapper/CloudFox | OS Evidence와 직접 맞지 않음 | 제외 | 미적용 |
| PostgreSQL로 저장소 교체 | 현재 Supabase가 요구를 충족하므로 불필요 | 제외 | 미적용 |
| LLM 생성 코드 즉시 실행 | root/Host 경계에서 위험 | 금지 | 미적용 |

## 5. 목표 아키텍처

~~~text
Frontend
  └─ Scenario / Campaign 요청
       ↓
Local Control Backend
  ├─ Scenario Guardrail
  ├─ Campaign Coordinator
  ├─ Agent State / Budget
  ├─ Frontier Builder
  ├─ Planner Adapter
  ├─ Independent Verifier Client
  ├─ Reset Coordinator
  └─ Supabase Repository
       ↓ SSM tunnel
EC2 root-owned Supervisor
  ├─ Profile allowlist 검증·적용
  ├─ Container/Host Runtime 시작
  ├─ 검증용 read-only snapshot 수집
  └─ 기준 상태 Reset
       ↓
Environment Runtime Agent
  ├─ Recon Tool Registry
  ├─ Action Tool Registry
  ├─ 구조화 Tool 인자 검증
  └─ 실제 OS operation 수행
~~~

책임 분리는 다음과 같다.

- Control Backend: State, Frontier, Campaign, Budget, 결과 저장
- Planner: 현재 State와 Frontier에서 Candidate 하나 선택 또는 종료 제안
- Runtime Agent: 선택된 Tool을 실제 환경 안에서 실행하고 원시 결과 반환
- Supervisor: Profile 적용, 별도 검증 Snapshot, Reset 수행
- Independent Verifier: Action 결과와 별도 Snapshot을 대조하여 VERIFIED, REJECTED, INCONCLUSIVE 판정

## 6. 공통 데이터 계약

### 6.1 OS Asset

~~~text
asset_id
asset_type: host | container | user | group | file | service | mount | capability | sudo_rule
resource_id
attributes
relationships
evidence_refs
~~~

실제 절대 경로는 LLM State에 포함하지 않고 profile-canary, nginx-target 같은 승인 Resource ID만 제공한다.

### 6.2 Authorization Observation

~~~text
observation_id
domain: os
subject_id
operation: read | write | status | execute_helper
resource_id
status: confirmed | denied | conditional_or_unknown
errno_or_error_code
evidence_refs
observed_at
~~~

선언된 Profile과 실제 관측을 분리한다.

- Profile 선언: owner_write=true
- 실제 관측: agent-host가 profile-canary 쓰기에 성공함
- Capability 확정: 필요한 모든 관측과 자산 관계가 충족됨

### 6.3 Capability

초기 Capability는 다음으로 제한한다.

| Capability | Container 조건 | Host 조건 |
| --- | --- | --- |
| CAN_READ_CANARY | mount 및 파일 read 가능 | 파일 read 가능 |
| CAN_WRITE_CANARY_DIRECT | mount_write와 유효 UID/capability | owner 또는 group write |
| CAN_WRITE_CANARY_VIA_HELPER | 미지원 | 제한된 sudo helper 성공 |
| CAN_BYPASS_DAC | root 또는 CAP_DAC_OVERRIDE 관측 | 초기 범위 제외 |
| CAN_QUERY_TARGET_SERVICE | 고정 service endpoint 접근 | 고정 service endpoint 접근 |

Capability 상태는 다음을 사용한다.

~~~text
unavailable
inferred
confirmed
denied
conditional_or_unknown
~~~

### 6.4 Action Candidate

~~~text
candidate_id
tool_name
arguments
target_resource
required_capabilities
required_evidence
risk_level
expected_state_change
alternative_on_failure
~~~

Candidate ID와 Tool·인자·Resource가 정확히 일치할 때만 실행한다. Planner가 Candidate에 없는 Tool이나 인자를 추가하면 Run을 중단한다.

### 6.5 Evidence와 Verification

~~~text
evidence_id
source: runtime | supervisor_snapshot | verifier | audit
resource_id
operation
observed_state
sha256
uid / gid / mode / capabilities
run_id
collected_at
~~~

Verification 결과는 다음으로 통일한다.

~~~text
VERIFIED
REJECTED
INCONCLUSIVE
~~~

기존 UI와 과거 로그 호환을 위해 저장 시 다음처럼 매핑한다.

- VERIFIED → 기대 조건 충족 시 PASS
- REJECTED → FAIL
- INCONCLUSIVE → INCONCLUSIVE

### 6.6 Reset Result

~~~text
reset_id
action_id
status: RESET | RESET_FAILED | NOT_REQUIRED
restored_state
evidence_refs
~~~

### 6.7 Budget와 종료 상태

~~~text
max_iterations
max_tool_calls
max_transient_retries
max_elapsed_seconds
max_no_progress_iterations
used_iterations
used_tool_calls
termination_reason
~~~

초기 기본값은 작게 고정한다.

~~~text
max_iterations = 6
max_tool_calls = 12
max_transient_retries = 1
max_elapsed_seconds = 120
max_no_progress_iterations = 2
~~~

## 7. 단계별 구현 계획

### 7.0 현재 착수 범위

Harness Core는 지금 착수한다. Domain 기능이 없어도 상태·예산·이벤트·종료·Port 계약과 API는 구현할 수 있다.

| 지금 구현 | 후속 Adapter로 연결 |
| --- | --- |
| Harness Run/State/Event 모델 | 실제 OS Permission Provider |
| Budget와 종료 상태 | 최종 Tool Catalog/Executor |
| 구성요소 readiness 검사 | Independent Verifier |
| In-memory Harness Repository | Action Resetter |
| Harness API와 Fake Adapter 테스트 | Capability/Frontier의 OS 규칙 |

필수 Adapter가 없는 상태의 실행은 BLOCKED로 종료한다. 이 동작 자체가 Harness가 미완성 기능을 실행하지 않는 안전장치다.

#### 2026-08-24 Harness Core 적용 결과

다음 Core가 실제 코드에 적용됐다.

- backend/app/harness/models.py: Run, State, Candidate, Budget, Event, Verification, Reset 계약
- backend/app/harness/ports.py: Permission Provider, Tool Catalog, Planner, Executor, Verifier, Resetter Port
- backend/app/harness/coordinator.py: readiness, Budget, 실행 수명주기와 종료 처리
- backend/app/harness/repository.py: 기존 Supabase Run과 분리된 In-memory Harness 저장
- backend/app/harness/fixtures.py: 메모리 전용 권한 Profile 4개, Tool 3개, Planner, Verifier, Resetter 예제
- GET /api/harness/status: 구성요소 연결 상태 조회
- POST /api/harness/runs: Harness Run 생성·실행
- GET /api/harness/runs/{run_id}: Harness Run 조회
- /api/health의 harness_api_version: os-harness-v1

현재 실제 OS Adapter는 의도적으로 연결하지 않았다. 따라서 기본 Harness Run은 BLOCKED와 MISSING_REQUIRED_COMPONENTS를 반환한다. 테스트에서는 fixture-container/host read-only/write Profile과 fixture_file_read/write/service_status Tool을 사용해 허용·거부·검증·Reset 전체 수명주기를 실행한다. Fixture는 메모리만 사용하고 Production 기본 앱에는 주입하지 않는다. 기존 /api/runs, Runtime, Supervisor, Terraform, SSM, Supabase 코드는 Harness 실행에 사용하거나 변경하지 않았다.

### Phase -1. OS Agent 비회귀 기준 고정 — P0 선행조건

#### 목표

Harness를 추가하면서 OS Agent의 확정된 장점과 외부 계약을 실행 가능한 회귀 기준으로 고정한다.

#### 구현 내용

1. 현재 Backend 테스트, Frontend lint/build, Terraform validate 결과를 기준선으로 기록한다.
2. 다음 안정된 운영 계약을 fixture 또는 contract test로 고정한다.
   - POST /api/runs 요청·응답
   - RuntimeDispatchRequest와 RuntimeAgentResult
   - Supervisor Unix socket 요청 형식
   - Supabase v1/v2 Run 복원
   - 배포·SSM API와 7단계 Workflow 상태
3. 신규 Harness API는 기존 POST /api/runs와 별도 경로로 추가한다.
4. 권한·Tool·Verifier는 Optional Port로 선언하고 Core에서 구체 구현을 import하지 않는다.
5. 모든 신규 DB 변경은 add-only migration으로 작성한다.

#### 완료 조건

- 기존 API 응답과 실행 결과가 변경 전과 동일하다.
- 기존 setup.cmd, run.cmd, 배포, SSM, 단일 Run, 로그 조회·삭제가 모두 동작한다.
- 기준선 테스트가 실패하면 이후 Phase를 병합하지 않는다.

### Phase 0. 공통 계약과 Tool Registry 정리 — P0

#### 목표

기존 단일 실행 동작을 바꾸지 않으면서 이후 Recon/Action/Verifier/Reset이 공통 계약을 사용하게 한다.

이 Phase에서는 구체 Tool을 등록하지 않고 ToolCatalog, Executor, Verifier, Resetter Port와 구성요소 readiness만 구현한다. 실제 OS Tool 등록은 후속 Adapter 작업에서 수행한다.

#### 구현 내용

1. backend/app/tools 패키지를 만든다.
2. ToolDefinition을 추가한다.
   - name
   - kind: recon 또는 action
   - 입력 JSON Schema
   - risk_level
   - resource_fields
   - handler
   - verifier
   - resetter
3. 현재 세 Tool의 구현을 이동하거나 재작성하지 않고, 기존 호출을 감싸는 Registry Adapter를 추가한다.
4. Tool 오류를 구조화한다.
   - ACCESS_DENIED
   - NOT_FOUND
   - INVALID_INPUT
   - TIMEOUT
   - EXECUTION_ERROR
5. profile_version을 실제 값으로 고정한다.
6. 신규 Run 결과 형식을 os-agent-v3로 추가하고 v1/v2 읽기 호환을 유지한다.
7. 기존 POST /api/runs와 Runtime dispatch payload는 변경하지 않는다.

#### 수정 예상 파일

- backend/app/schemas.py
- backend/app/catalog.py
- backend/runtime_agent/runtime.py
- 신규 backend/app/tools/registry.py
- 신규 backend/app/tools/contracts.py
- backend/tests/test_api.py
- 신규 backend/tests/test_tool_registry.py

#### 완료 조건

- 기존 세 Tool의 정상·거부 동작이 변경되지 않는다.
- Feature flag를 끄면 Registry Adapter를 거치지 않는 기존 경로가 그대로 동작한다.
- 등록되지 않은 Tool, 추가 인자, 임의 Resource ID가 Runtime 실행 전에 거부된다.
- 모든 Tool 결과가 동일한 구조화 오류와 Evidence envelope를 반환한다.

### Phase 1. 읽기 전용 OS Recon State — P0

#### 목표

Agent가 Action 전에 실제 실행 경계를 이해할 수 있는 최소 State를 만든다.

#### 신규 Recon Tool

| Tool | 반환 범위 | 안전 제한 |
| --- | --- | --- |
| identity_snapshot | uid, gid, group ID, container/host 경계 | 사용자명 대신 승인된 논리 ID 사용 가능 |
| file_metadata | Canary owner, group, mode, hash | profile-canary만 허용 |
| mount_status | read-only/read-write, mount target | Container에서만, 승인 mount만 허용 |
| capability_status | effective capability 집합 | Container Runtime 자신만 조회 |
| sudo_helper_status | 고정 helper 실행 허용 여부 | sudoers 원문 반환 금지 |
| service_status | nginx-target 상태 | 기존 고정 대상 유지 |

#### Recon Scheduler

초기에는 LLM이 Recon 순서를 고르지 않는다. Harness가 경계에 따라 안전한 읽기 전용 Tool을 결정론적으로 실행한다. Recon은 기존 Profile 적용 이후 같은 Runtime 경계 안에서 수행하며, 새로운 root 권한이나 외부 Network 목적지를 요구하지 않는다.

~~~text
Container:
identity_snapshot → file_metadata → mount_status → capability_status → service_status

Host:
identity_snapshot → file_metadata → sudo_helper_status → service_status
~~~

실패한 Recon은 권한이 없다고 자동 확정하지 않는다. 안정적인 EACCES/EPERM만 denied로 기록하고, timeout·파싱 오류는 conditional_or_unknown 또는 INCONCLUSIVE로 기록한다.

#### 수정 예상 파일

- 신규 backend/app/agent_state.py
- 신규 backend/app/recon.py
- 신규 backend/runtime_agent/recon_tools.py
- backend/app/schemas.py
- backend/app/executor.py
- backend/host_runtime/host_supervisor.py
- 신규 backend/tests/test_recon.py
- data/schema.sql

#### 완료 조건

- Action 실행 전 State에 Identity와 Canary 관계가 생성된다.
- Profile 선언과 실제 mode/uid/gid/capability 관측이 별도 필드로 남는다.
- LLM Context에는 실제 경로, sudoers 원문, 환경변수와 Credential이 포함되지 않는다.
- Recon을 켜거나 꺼도 기존 Profile 적용 결과와 세 Tool의 실제 권한 효과가 같다.

### Phase 2. Capability, Frontier와 단일 Agent Re-plan — P0

#### 목표

정찰 결과를 바탕으로 실행 가능한 후보를 만들고, Agent가 후보 중 하나를 선택하게 한다.

#### 구현 내용

1. CapabilityRule과 CapabilityAssessment를 구현한다.
2. 현재 자산·권한 관측으로 ActionFrontier를 생성한다.
3. Planner 입력을 다음으로 제한한다.
   - Campaign 목표
   - 정규화된 OS State
   - 현재 Frontier
   - 이전 실행의 구조화 결과
   - 남은 Budget
4. Planner는 candidate_id 하나 또는 종료만 반환한다.
5. Tool Registry가 Candidate의 Tool·인자·Resource 일치를 재검증한다.
6. 실행 결과를 State에 반영하고 Frontier를 다시 계산한다.

#### 초기 Frontier 규칙

- 읽기 Evidence가 없으면 file_read Candidate를 우선 생성한다.
- service 상태 Evidence가 없으면 service_status Candidate를 생성한다.
- 쓰기 Capability가 inferred 또는 conditional_or_unknown이면 file_write Candidate를 생성한다.
- 동일 Tool·Resource·인자 효과가 이미 VERIFIED 또는 안정적으로 denied이면 중복 Candidate를 제거한다.
- INVALID_INPUT은 권한 실패로 기록하지 않고 해당 Candidate만 폐기한다.

#### 수정 예상 파일

- 신규 backend/app/capabilities.py
- 신규 backend/app/frontier.py
- 신규 backend/app/planner.py
- 신규 backend/app/campaign.py
- backend/app/executor.py
- backend/runtime_agent/runtime.py
- 신규 backend/tests/test_capabilities.py
- 신규 backend/tests/test_frontier.py
- 신규 backend/tests/test_campaign.py

#### 완료 조건

- 결정론적 Planner로 전체 Loop를 모델 없이 테스트할 수 있다.
- OpenRouter Planner는 같은 Candidate 계약을 사용한다.
- 첫 Tool 실패 후 다른 유효 Candidate로 재계획한다.
- 임의 Tool/Resource/인자 생성은 불가능하다.

### Phase 3. Independent Evidence와 Action별 Reset — P0

#### 목표

Runtime Agent가 반환한 값과 독립적으로 실제 효과를 재확인하고, 기준 상태 복구를 다음 Action의 선행조건으로 만든다.

#### 검증 채널

Supervisor의 기존 Profile 적용과 Runtime 실행 API는 유지한다. 그 옆에 승인 Resource의 상태만 읽는 검증 요청을 별도로 추가하며, 이 요청은 Profile 변경이나 임의 명령 실행을 수행할 수 없다.

~~~text
Runtime Action 결과
        ↓
Control Backend가 verification request 생성
        ↓
Supervisor가 승인 Resource의 실제 상태를 새로 수집
        ↓
Independent Verifier가 Action 결과와 Snapshot 비교
~~~

검증 Snapshot에는 필요한 최소 정보만 포함한다.

- Canary hash
- owner UID/GID
- mode bit
- Container mount mode
- Runtime UID/capability 적용 상태
- Host group membership 적용 상태
- 제한 sudo helper 활성 여부
- nginx-target health

#### Reset 계약

- file_read, service_status: NOT_REQUIRED
- file_write: 초기 Canary 내용·owner·group·mode를 복구하고 hash 재확인
- Profile: Run 종료 시 group membership, sudoers drop-in, file mode, Container 상태 복구
- Reset 실패 시 Campaign을 즉시 중단하고 RESET_FAILED 기록

#### 수정 예상 파일

- backend/app/verifiers.py
- 신규 backend/app/evidence.py
- 신규 backend/app/reset.py
- backend/app/runtime_client.py
- backend/host_runtime/host_supervisor.py
- infra/terraform/user_data.sh.tpl
- infra/terraform/runtime-compose.yml.tpl
- backend/tests/test_verifiers.py
- 신규 backend/tests/test_reset.py

#### 완료 조건

- Runtime 결과가 성공이어도 별도 Snapshot이 다르면 REJECTED 처리된다.
- Verifier Evidence ID가 Runtime Evidence ID와 다르다.
- 상태 변경 Action 뒤 Reset 성공 Evidence가 없으면 다음 Candidate를 실행하지 않는다.
- Container와 Host 각각에서 Profile 잔여 상태가 다음 Run에 남지 않는다.
- 기존 Control Verifier의 PASS/FAIL/INCONCLUSIVE 의미와 과거 로그 표시는 유지된다.

### Phase 4. Scenario Guardrail, Campaign Budget와 종료 — P1

#### 목표

여러 Action을 반복해도 실행 범위와 비용이 고정되도록 한다.

#### Scenario 입력

~~~text
scenario_id / version
subject_mode
authorized_host_id
allowed_tools
allowed_resources
max_risk_level
budget
reset_after_action
~~~

초기에는 Scenario를 서버의 버전 관리된 JSON 또는 Python 상수로만 제공한다. Frontend가 임의 Scenario JSON을 제출하게 하지 않는다.

#### 종료 사유

- CAMPAIGN_COMPLETED: 유효 Frontier 소진
- GOAL_VERIFIED: 명시된 실험 목표 검증
- TOOL_BUDGET_EXHAUSTED
- ITERATION_BUDGET_EXHAUSTED
- TIME_BUDGET_EXHAUSTED
- NO_PROGRESS
- RESET_FAILED
- GUARDRAIL_REJECTED
- RUNTIME_ERROR

#### 수정 예상 파일

- 신규 backend/scenarios/os-minimum-v1.json
- 신규 backend/app/scenario.py
- backend/app/campaign.py
- backend/app/schemas.py
- backend/app/main.py
- 신규 backend/tests/test_scenario.py
- backend/tests/test_api.py

#### 완료 조건

- Scenario 밖 Tool, Resource, Host, 위험도 요청이 Supervisor 호출 전에 거부된다.
- 모든 종료가 구조화된 termination_reason을 가진다.
- 재시도는 Tool이 retryable=true로 반환한 transient 오류에만 적용된다.

### Phase 5. 저장·API·대시보드 확장 — P1

#### 목표

기존 운영 화면을 유지하면서 Agent의 판단과 실제 Evidence를 분리해 확인할 수 있게 한다.

#### Supabase 변경

기존 runs, run_events를 유지하고 다음을 추가한다.

~~~text
runs.agent_state jsonb
runs.capabilities jsonb
runs.frontier jsonb
runs.budget jsonb
runs.termination_reason text
runs.scenario_id text
runs.scenario_version text

run_actions
  action_id
  run_id
  candidate_id
  tool
  arguments
  status
  verification
  reset_result

run_evidence
  evidence_id
  run_id
  action_id
  source
  evidence_type
  payload
  created_at
~~~

Evidence payload 저장 전에 실제 경로, 환경변수, Token, Authorization header를 제거한다.

#### API 변경

- POST /api/campaigns
- GET /api/runs/{run_id}/state
- GET /api/runs/{run_id}/actions
- GET /api/runs/{run_id}/evidence
- 기존 POST /api/runs는 단일 실행 호환 API로 유지

#### UI 변경

기존 환경 배포·SSM·워크플로우 화면과 조작 순서를 변경하지 않고, 실행 상세에 다음 탭만 추가한다.

- 개요: 목표, 경계, Profile, 최종 판정, 종료 사유
- 정찰 State: Identity, 파일, mount, group, sudo, service
- Capability: 상태와 충족·누락 조건
- Action 경로: Frontier 선택과 Re-plan 순서
- Evidence: Runtime/Verifier/Reset Evidence 분리
- Budget: 반복·Tool·재시도·시간 사용량

#### 수정 예상 파일

- data/schema.sql
- data/migrations
- backend/app/repository.py
- backend/app/main.py
- frontend/src/types.ts
- frontend/src/api.ts
- frontend/src/components/RunResult.tsx
- frontend/src/components/OsResultDetailPage.tsx
- frontend/src/components/EventTimeline.tsx
- 신규 Capability/Graph/Evidence 컴포넌트

#### 완료 조건

- 과거 v1/v2 Run을 계속 조회할 수 있다.
- 신규 Campaign은 각 Action, Verification, Reset을 같은 run_id로 조회할 수 있다.
- UI에서 Agent 주장, Runtime 결과, Verifier 판정이 시각적으로 분리된다.
- 기존 배포·EC2 선택·SSM·환경 삭제·로그 삭제 버튼의 조건과 확인 절차가 바뀌지 않는다.

### Phase 6. 선택적 Multi-Agent — P2

#### 도입 조건

다음 조건이 모두 충족된 뒤에만 시작한다.

- 결정론적 Planner Campaign 테스트 통과
- OpenRouter 단일 Planner E2E 통과
- 모든 상태 변경 Tool의 Verifier/Reset 연결
- Budget과 종료 사유 검증
- 실제 Container/Host 조건 반복 재현

#### 최소 역할

- Explorer A: 파일 소유자·mode 기반 경로 제안
- Explorer B: group·sudo·capability 기반 대안 경로 제안
- Critic: Evidence와 누락 선행조건 비교
- Supervisor: Candidate 계약과 Budget을 결정론적으로 재검증

역할별 Agent가 직접 Tool을 호출하지 않는다. 모든 제안은 동일 Frontier Candidate ID를 사용하며 최종 실행은 기존 Campaign Coordinator 하나만 수행한다.

#### 도입하지 않을 기능

- Agent끼리 자유 형식 shell 명령 교환
- Agent가 새로운 root helper 생성
- Agent가 Scenario 범위 변경
- 자동 생성 Tool의 즉시 등록
- 여러 Host로 자동 확산

## 8. 권장 PR 순서

H-Core는 지금 적용하는 공통 Harness 작업이고, F 단계는 나중에 연결할 OS Domain 기능이다.

| PR | 범위 | 선행 조건 | 핵심 산출물 |
| --- | --- | --- | --- |
| H-Core | Harness Core와 API | 완료 | Run/State/Event/Budget/Port/readiness/In-memory 저장 |
| H-0 | 비회귀 계약 고정 | H-Core | 기존 API·Runtime·Supervisor·배포 contract test |
| F-1 | OS 권한 모델 확정 | 후속 | Profile·적용 상태·비교 단위·Reset 기준 |
| F-2 | OS Tool 계약 확정 | F-1 | 최소 Tool·Schema·allowlist·오류·위험도 |
| F-3 | OS Verifier 계약 확정 | F-1~2 | 독립 Evidence·판정·Reset 검증 |
| H-1 | Harness Domain Adapter | H-0, F-1~3 | 완성된 OS 기능을 Harness Port에 연결 |
| H-2 | Recon State | H-1 | Identity/File/Mount/Capability/Sudo Recon |
| H-3 | Capability와 Frontier | H-2 | 규칙 평가, 결정론적 Planner |
| H-4 | Independent Evidence 연결 | H-1~3 | 완성된 Verifier를 Harness 실행 흐름에 연결 |
| H-5 | Campaign/Scenario/Budget | H-3~4 | 반복 Re-plan과 종료 상태 |
| H-6 | Supabase/API/UI | H-5 | Action·Evidence·Graph 조회 화면 |
| H-7 | Multi-Agent 실험 | H-1~6 E2E 완료 | 필요성이 입증된 경우에만 Explorer/Critic/Supervisor |

한 PR에서 DB, Runtime, Supervisor, UI를 모두 동시에 바꾸지 않는다. API 계약을 먼저 추가하고 구버전 호환을 유지한 다음 소비자를 순차적으로 전환한다. 모든 H 단계 PR은 Harness 기능 테스트뿐 아니라 H-0에서 고정한 기존 OS Agent 회귀 테스트를 함께 통과해야 한다.

## 9. 테스트 계획

### 9.1 단위 테스트

- Tool Schema 필수값·타입·추가 필드 거부
- Resource allowlist와 path traversal 거부
- errno와 구조화 오류 매핑
- Asset/Authorization State 병합과 중복 제거
- Capability 상태 계산
- Frontier 후보 생성과 중복 제거
- Candidate ID/Tool/인자 불일치 거부
- Budget 증가와 종료 사유
- Verification Evidence 독립성
- Reset 실패 시 Campaign 중단
- 민감정보 Redaction

### 9.2 통합 테스트

- Container 전체 OFF/ON Profile 조합
- Host 전체 OFF/ON Profile 조합
- Recon → Frontier → file_write → Verify → Reset → Re-plan
- 첫 Action ACCESS_DENIED 후 대안 Candidate 선택
- Runtime 성공/Verifier 불일치 시 REJECTED
- SSM tunnel을 통한 원격 Campaign
- Supabase 저장 후 State/Action/Evidence 재조회

### 9.3 반복 재현 테스트

각 Profile 조건을 최소 3회 반복하고 다음을 확인한다.

- 동일 초기 State
- 동일 Candidate 집합
- 기대한 허용·거부 결과
- 동일 최대 Capability
- Reset 후 Canary hash·mode·owner·group 복원
- 이전 Run의 group, sudoers, Container, capability 상태 미잔류

### 9.4 보안 테스트

- ../, 절대 경로, symlink 우회 차단
- 임의 service 이름 차단
- NUL과 128자 초과 입력 차단
- 임의 sudo 명령 차단
- Runtime payload의 Profile key 위조 차단
- 다른 run_id, subject_mode, profile_id 응답 차단
- Evidence/로그의 API Key, AWS Credential, 환경변수 제거
- Planner가 Registry 밖 Tool을 요청할 때 실행되지 않음

### 9.5 OS Agent 장점 보존 테스트

- 기존 단일 Run 요청·응답 Snapshot 동일
- Container mount/UID/capability의 실제 적용 주체와 결과 동일
- Host owner/group/sudo의 실제 적용 주체와 결과 동일
- Backend와 Runtime에 Docker socket, privileged, 신규 sudo 권한이 추가되지 않음
- Supervisor allowlist와 Unix socket 권한이 약화되지 않음
- private subnet, public inbound 없음, SSM 전용 연결 유지
- setup.cmd와 run.cmd 실행 방식 유지
- Terraform deploy/destroy와 환경별 state 관리 유지
- Supabase 미설정 시 메모리 fallback 유지
- 기존 로그 조회·단일 삭제와 7단계 Workflow 유지
- 기존 비용 경고, 삭제 확인 문자열과 실행 중 버튼 잠금 유지

## 10. 전체 완료 기준

- [x] Harness Core가 Domain 기능과 분리된 Port 구조로 구현된다.
- [x] 필수 Adapter가 없으면 BLOCKED와 누락 구성요소를 반환한다.
- [ ] H-0 비회귀 계약이 구현된다.
- [ ] 기존 OS Agent 동작이 변경되지 않는다.
- [ ] 실제 Container/Host 실행 위치와 root Supervisor 책임이 유지된다.
- [ ] 배포·SSM·Terraform·Supabase·Windows 실행 장점이 유지된다.
- [ ] Agent가 Action 전 읽기 전용 OS Recon을 수행한다.
- [ ] 선언 Profile과 실제 OS 관측이 분리되어 저장된다.
- [ ] 최소 5개 OS Capability가 구조화된다.
- [ ] 현재 State에서 Action Frontier가 결정론적으로 생성된다.
- [ ] Planner는 Frontier Candidate만 선택할 수 있다.
- [ ] 실패 결과를 반영해 최소 한 번 이상 Re-plan할 수 있다.
- [ ] Runtime Evidence와 독립 Verifier Evidence가 분리된다.
- [ ] 모든 상태 변경 Action에 Reset이 연결된다.
- [ ] Reset 실패 시 다음 Action이 실행되지 않는다.
- [ ] Scenario가 Host·경계·Tool·Resource·위험도·Budget을 고정한다.
- [ ] Campaign 종료 사유가 구조화되어 저장된다.
- [ ] Supabase와 UI에서 State, Capability, Action, Evidence, Reset을 조회할 수 있다.
- [ ] 기존 단일 Run과 과거 로그 조회 호환이 유지된다.
- [ ] Container와 Host 실제 환경에서 동일 조건 3회 반복 결과가 재현된다.
- [ ] Credential, 실제 경로, sudoers 원문과 민감 환경변수가 LLM/로그에 노출되지 않는다.

기능 체크리스트를 모두 만족하더라도 위 네 개 보존 항목 중 하나가 실패하면 전체 완료로 판정하지 않는다.

## 11. 우선 구현 권고

지금은 Harness Core와 API를 먼저 구현한다. 나중에 F-1~F-3의 권한·Tool·Verifier 계약이 완성되면, 가장 먼저 연결할 수직 경로를 다음 하나로 제한한다. 이 경로 전에 H-0으로 Host 단일 Run의 입력·출력·권한 효과를 고정한다.

~~~text
Host Profile 적용
→ identity_snapshot
→ file_metadata
→ CAN_WRITE_CANARY_DIRECT 추론
→ file_write Candidate 생성
→ Planner 선택
→ 실제 file_write
→ Supervisor 독립 hash/mode/owner Snapshot
→ Verifier 판정
→ Canary와 Profile Reset
→ State/Action/Evidence Supabase 저장
~~~

이 경로가 완료되면 같은 계약을 Container mount/root/capability와 Host group/sudo 경로로 확장한다. 첫 Harness 구현에서 Tool 수나 Agent 수를 늘리는 것보다 하나의 Action이 정찰·추론·선택·검증·복구까지 완전히 닫히는 것이 우선이다.

현재 시점의 결론은 다음과 같다.

> Agent Harness Core는 지금 적용하고, OS 권한·Tool·Verifier는 확정되는 순서대로 Adapter로 연결한다.

## 12. 참고 자료

### OS 팀 자료

- README.md
- OS-최소환경테스트-계획.md
- backend/app/schemas.py
- backend/app/executor.py
- backend/runtime_agent/runtime.py
- backend/host_runtime/host_supervisor.py
- backend/app/verifiers.py
- data/schema.sql

### 다른 팀 구현

- GitHub Ai-Agent feat/first-demo, commit 0a5ebe2de9c636339b1c9dc2ec1c43ca3723bff9
- GitHub Ai-Agent feat/aws-service-tools, commit 46c646773e059ff385e8f5067fbd6e73a7744be7
- Ai-Agent main은 문서 골격이며, 위 기능은 원격 기능 브랜치 기준이다.
- 공통 Core: State, Recon Scheduler, Capability, Frontier, Planner, Multi-Agent, Verifier, Reset, Scenario, Budget
- EKS 기능은 실행환경 차이 때문에 구조와 계약만 참고하고 Kubernetes/AWS Tool 자체는 적용 대상에서 제외

### ECR-ECS 팀 설계 참고

- ../team-hub/notes/topics/ecr-ecs/README.md
- ../team-hub/notes/topics/ecr-ecs/ECR-ECS-아키텍처.md
- ../team-hub/notes/topics/ecr-ecs/ECR-ECS-구조(easy-ver).md
